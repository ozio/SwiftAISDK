# AI Upstream Test Checklist

Snapshot:

- Upstream package: `ai@6.0.208`
- Upstream ref: `vercel/ai@184dc39c2b2cf8cb9302d81f87edcf2f665cfd8c`
- Local package: SwiftAISDK core AI/facade/util surfaces

This file tracks the core `ai` package from `Docs/TestPortingPlan.md`. The
package is large, so the checklist is advanced in coherent slices. Every
upstream file should eventually end in one of these states: `ported`,
`covered-by-existing-platform-test`, `out-of-scope`, or `blocked`.

## Ported

| Upstream file | Swift evidence | Notes |
| --- | --- | --- |
| `packages/ai/src/generate-text/calculate-tokens-per-second.test.ts` | `AiCoreUtilityUpstreamTests.swift` | Covered as Swift `calculateTokensPerSecond(tokens:durationMilliseconds:)`; uses `Double?` to preserve upstream `Infinity` / `NaN` cases. |
| `packages/ai/src/generate-text/collect-tool-approvals.test.ts` | `AiCollectToolApprovalsUpstreamTests.swift`, `ToolPreparation.swift` | Covered as Swift `collectToolApprovals(messages:)` over `AIMessage`: non-tool final messages and missing responses produce no approvals; approved/denied responses are collected with their request and tool call; approvals that already have a tool result are ignored; unknown approval IDs and missing referenced tool calls throw typed errors; mixed approved/denied/processed batches preserve ordering. |
| `packages/ai/src/generate-text/execute-tool-call.test.ts` | `AiExecuteToolCallUpstreamTests.swift`, `AIToolExecution.swift`, `CoreTools.swift` | Covered through Swift `executeToolCalls(...)` single-call behavior: successful execution returns an `AIToolResult`, provider metadata is preserved, messages/abort signal/provider metadata/validated tool context are passed to `AIToolExecutionContext`, invalid tool context throws `AITypeValidationError` before the tool body runs, execution errors propagate as Swift errors, dynamic tools produce dynamic results, and user-approval requests skip execution. Upstream JS no-execute-tool passthrough, returned `tool-error` objects, preliminary async-generator outputs, callback arrays, sandbox parameter, explicit `executeToolInTelemetryContext`, and timeout-object branches are runtime/API-shape-inapplicable or covered by Swift telemetry/abort/facade tests because Swift tools are executable closures and tool errors are represented by thrown errors. |
| `packages/ai/src/generate-text/execute-tools-from-stream.test.ts` | `AiExecuteToolsFromStreamUpstreamTests.swift`, existing stream facade tests | Covered through Swift `AI.streamText(... executableTools:)`, the port's equivalent streaming tool-execution surface: streamed tool calls are forwarded, executable tools produce tool results after model finish, provider-executed tool calls and provider results are forwarded without invoking local tools, automatic approved/denied approvals emit request/response parts and skip denied tool bodies, invalid per-tool context throws `AITypeValidationError` before `needsApproval` or tool execution, and tool execution errors are returned as error `AIToolResult` stream parts while still recording tool error telemetry. Upstream's standalone `ReadableStream` transform, sandbox parameter, explicit start/end callback arrays, fake timer execution-duration snapshots, and tools without `execute` are API-shape-inapplicable to the Swift facade/runtime; denied approval synthetic `execution-denied` results are covered at the Swift high-level facade layer, matching upstream `generateText` / `streamText` response-message behavior rather than the internal transform snapshot. |
| `packages/ai/src/generate-text/filter-active-tools.test.ts` | `AiFilterActiveToolsUpstreamTests.swift`, `ToolPreparation.swift` | Covered as Swift `filterActiveTools(tools:activeTools:)` over the port's typed tool dictionary representation: nil tools return nil, nil active tools keep all tools, empty active tools return an empty dictionary, and named filtering preserves function and provider-defined tool entries. |
| `packages/ai/src/generate-text/invoke-tool-callbacks-from-stream.test.ts` | `AiInvokeToolCallbacksFromStreamUpstreamTests.swift`, `CoreTools.swift`, `AITokenUsageAndStreamRuntime.swift` | Covered as Swift `AITool.onInputStart`, `onInputDelta`, and `onInputAvailable` callbacks invoked from `forwardLanguageStream(...)`: the stream is passed through unchanged, callbacks fire in upstream order for `toolInputStart`, each `toolInputDelta`, and final `toolCall`, and callback contexts carry tool call ID, request messages, abort signal, parsed input for availability, and per-tool `toolContext`. Upstream `runtimeContext` is represented by Swift's existing per-tool `LanguageModelRequest.toolContexts`; arbitrary JS callback option object shapes are mapped to typed Swift callback context structs. |
| `packages/ai/src/generate-text/output.test.ts` | `AiOutputUpstreamTests.swift`, existing `AIObjectFacadeGenerationAndStreamingTests.swift`, existing `AIObjectFacadeOutputStrategyTests.swift` | Covered through Swift `Output.text`, `Output.object`, `Output.array`, `Output.choice`, and `Output.json` facade behavior plus internal response-format and partial parsing helpers: text output sets `.text` response format and returns/streams text, object output streams valid partial text fragments plus incremental partial/final output, object/json partial parsing follows `parsePartialJSON`, object/schema failures validate raw JSON before Swift decoding, array envelopes use upstream `elements` schema and stream complete-object partials for both multi-chunk and single-chunk arrays, repaired array partials drop the last potentially incomplete element, and choice partials suppress ambiguous prefixes, deduplicate repeated resolved prefixes, and resolve exact or single-prefix matches. Upstream standalone `parseCompleteOutput` / `parsePartialOutput` object APIs are represented by Swift generate/stream facade and internal helpers; JS `undefined as any`, arbitrary non-string runtime inputs, and zod-transform static typing branches are API-shape-inapplicable to typed Swift calls. |
| `packages/ai/src/generate-text/parse-tool-call.test.ts` | `AiParseToolCallUpstreamTests.swift`, `AiStreamLanguageModelCallUpstreamTests.swift`, `AIToolExecution.swift` | Covered as Swift `parseToolCall(...)`, `AIToolCallRepair`, and the production `executeToolCalls(...)` / stream forwarding paths: valid input is parsed and schema-validated, `AITool.refineArguments` refines after parsing, provider-executed dynamic tool calls can parse without a registered tool, provider metadata is preserved, empty input maps to `{}`, missing tools throw `AINoSuchToolError`, invalid schema input throws `AIInvalidToolInputError`, registered dynamic tools are annotated, unknown tool names can be repaired before emission/execution, and refinement/repair failures throw `AIToolCallRepairError`. Upstream's invalid tool-call return object, tool title propagation, and separate `toolMetadata` field are API-shape-inapplicable to the current Swift runtime, which uses typed thrown errors, `AIToolCall.title`, and provider metadata. |
| `packages/ai/src/generate-text/prune-messages.test.ts` | `AiPruneMessagesUpstreamTests.swift`, `PruneMessages.swift` | Covered as Swift `pruneMessages(...)` over typed `AIMessage`: all/before-last reasoning pruning maps to `AIMessage.reasoning`, tool-call pruning removes calls/results/approval requests/responses for `all`, `beforeLastMessage`, `beforeLastMessages(_:)`, and per-tool settings, keeps tool parts associated with retained trailing messages, removes empty messages by default, preserves non-tool content, and covers the upstream selective approval-pruning regression so approval responses are pruned with their request/tool call and unresolved approval responses do not survive as authority context. Upstream string literal option types map to Swift enums/structs. |
| `packages/ai/src/generate-text/restricted-telemetry-dispatcher.test.ts` | `AiRestrictedTelemetryDispatcherUpstreamTests.swift`, existing telemetry tests | Covered through Swift's telemetry privacy gate instead of JS dynamic context allow-lists: `Telemetry.Options(includesInput: false, includesOutput: false)` redacts lifecycle, step, and tool event input/output while preserving function ID, metadata, usage, and tool execution wrapping. Upstream `runtimeContext` / `toolsContext` allow-list filtering and source-object mutation checks are API-shape-inapplicable because Swift telemetry events are typed value structs and the Swift request surface has no general runtime context channel; per-tool context is exposed to tool callbacks/execution rather than copied into telemetry by default. |
| `packages/ai/src/generate-text/resolve-tool-approval.test.ts` | `AiResolveToolApprovalUpstreamTests.swift`, `AIToolExecution.swift`, `CoreTools.swift`, `CoreLanguageRequest.swift` | Covered as Swift `resolveToolApproval(...)` and `executeToolCalls(...)`: no configured approval returns `.notApplicable`; generic `toolApproval` has precedence over tool-defined approval, receives tool call/input/tool/request/tool context from `LanguageModelRequest.toolContexts`, preserves approved/denied reasons, and maps nil to `.notApplicable` without falling through; tool-defined `AITool.needsApproval` maps boolean results to `.userApproval` / `.notApplicable`, receives input/messages/validated context, and schema failures throw `AITypeValidationError` before callback invocation; execute-time user approval requests skip tool execution. Upstream JS per-tool `toolApproval` object/static string shorthand is represented in Swift by typed generic `AIToolApproval` plus per-tool `AITool.needsApproval`, so those shorthand branches are API-shape-inapplicable rather than separately mirrored. Zod default application in `contextSchema` is not mirrored by Swift `AIJSONSchemaValidator`, which validates JSON Schema but does not synthesize default values. |
| `packages/ai/src/generate-text/smooth-stream.test.ts` | `AiSmoothStreamUpstreamTests.swift`, `CoreUtilities.swift` | Covered as Swift `smoothStream(...)`: word chunking combines partial words, splits large chunks, keeps long whitespace with word chunks, avoids whitespace-only chunks, and flushes before tool/non-smoothable parts; line chunking splits on newlines and flushes final unterminated lines; custom detector coverage mirrors upstream regex/callback behavior including non-zero match starts, character-level chunking, empty-match errors, and non-prefix errors; text part ID changes flush buffers; reasoning deltas are smoothed with word/line behavior, text/reasoning switches flush buffers, and provider metadata is preserved for reasoning signatures and passed-through starts. JS invalid string/null chunking checks, fake delay hook snapshots, first-class `RegExp`, and `Intl.Segmenter` object support are API-shape-inapplicable to the Swift typed enum/custom-detector surface. |
| `packages/ai/src/generate-text/stream-language-model-call.test.ts` | `AiStreamLanguageModelCallUpstreamTests.swift`, `AITokenUsageAndStreamRuntime.swift`, `AIToolExecution.swift`, existing telemetry tests | Covered through Swift `AI.streamText`, `forwardLanguageStream(...)`, `AIToolCallRepair`, and telemetry events: stream-start warnings and text/reasoning/file/reasoning-file/custom parts are forwarded with provider metadata; tool-call inputs are parsed/refined before emission and execution; unknown tool names can be repaired before emission/execution; provider approval requests must reference an existing prior tool call and are associated with the matched provider-executed call; multiple provider approvals are matched independently; telemetry start/end events share call IDs, carry request input, aggregate text/tool-call counts, usage, and response metadata. Upstream fake-clock performance snapshots, injectable ID generators, and internal callback content-array snapshots are API-shape-inapplicable to Swift's public stream/telemetry surface; Swift telemetry tests cover the public event path and duration recording. |
| `packages/ai/src/generate-text/to-response-messages.test.ts` | `AiToResponseMessagesUpstreamTests.swift`, `ResponseMessages.swift`, `CoreMessages.swift` | Covered as Swift `toResponseMessages(content:toolsByName:)`: text content becomes an assistant message; empty text and empty content are skipped; ordinary tool results are emitted in a tool message; provider-executed tool results stay in the assistant message; reasoning, reasoning-file, custom, file, and text parts preserve ordering and provider metadata; `AITool.toModelOutput` is applied to matching tool results; denied approval responses synthesize `execution-denied` tool results; and invalid dynamic tool-call input is sanitized to `{}` while valid object input is preserved. Swift keeps typed `AIContentPart` cases instead of upstream JS object unions, and provider-specific resend support remains covered by provider serializer tests. |
| `packages/ai/src/generate-text/stop-condition.test.ts` | `AiStopConditionUpstreamTests.swift`, existing `AIFacadeMediaEmbeddingAndUploadTests.swift` | Covered as Swift `AIStopCondition.isStepCount`, `isLoopFinished`, `hasToolCall`, and `isStopConditionMet`, including exact step-count matching, last-step-only tool call checks, any-matching tool names, no-step behavior, async stop conditions, and thrown condition propagation. |
| `packages/ai/src/generate-text/sum-token-counts.test.ts` | `AiCoreUtilityUpstreamTests.swift` | Covered as Swift `sumTokenCounts(_:_:)`; unknown counts map to `nil`. |
| `packages/ai/src/generate-text/tool-approval-signature.test.ts` | `AiToolApprovalSignatureUpstreamTests.swift`, `ToolPreparation.swift` | Covered as Swift `signToolApproval`, `verifyToolApprovalSignature`, and `maybeSignApproval`, including HMAC-SHA256 base64url signatures, approval ID/tool call ID/tool name/input/secret tamper rejection, canonical key ordering for equivalent inputs, and nil-secret no-op behavior. |
| `packages/ai/src/agent/tool-loop-agent.test.ts` | `AIAgentTests.swift`, `AiGenerateTextUpstreamTests.swift`, `AIFacadeStreamTextCoreTests.swift, AIFacadeStreamTextToolExecutionTests.swift, AIFacadeStreamTextToolLoopAggregationTests.swift`, `AIFacadeToolLoopTests.swift`, telemetry tests | Covered through Swift `AIToolLoopAgent` plus the underlying generate/stream tool-loop facade: agent version/id/default max steps, instructions prepending, request option forwarding, call-level option/header/provider-option merging, abort signal forwarding, generate and stream timeout behavior, `allowSystemInMessages` reject-by-default/opt-in behavior for generate and stream calls, stream telemetry forwarding, UI stream conversion, tool approval flows, tool execution context, stop conditions, and telemetry wrappers. JS `prepareCall`, sandbox/runtime context, `experimental_download`, object/array system-message instructions, `prepareCall` returning `allowSystemInMessages`, top-level agent `toolOrder`, user lifecycle callback aliases, global telemetry discovery, throwing-listener swallowing, and `callOptionsSchema` runtime validation are API-shape gaps for Swift's typed `AIAgentCallOptions`, `AIPrepareStep`, `AIChatRequestOptions`, and telemetry protocol surfaces; adjacent behavior is covered on the public Swift facade/helper APIs where those surfaces exist. |
| `packages/ai/src/telemetry/create-telemetry-dispatcher.test.ts` | `AiTelemetryDispatcherUpstreamTests.swift`, `AITelemetryRuntime.swift`, existing facade telemetry tests | Covered through Swift `TelemetryDispatcher`: no integrations make record calls no-op and language/tool execution pass through; local integrations receive recorded events in declaration order; `isEnabled: false` disables local and registered integrations; registered integrations are used when no local integrations are supplied; local integrations override registered integrations; language model and tool execution wrappers can surround and transform operation results. JS optional listener objects, deprecated callback aliases, dynamic class `this` binding, and swallowed throwing listener callbacks are API-shape-inapplicable because Swift uses a typed `Telemetry.Integration` protocol with non-throwing `record` and explicit throwing execution wrappers. |
| `packages/ai/src/telemetry/telemetry-registry.test.ts` | `AiTelemetryRegistryUpstreamTests.swift`, `Telemetry.swift` | Covered as Swift `Telemetry.register(...)`, `Telemetry.registeredIntegrations()`, and test-only `Telemetry.removeAllIntegrations()`: single integrations are registered, multiple registrations preserve call order, array registration preserves array order, zero-argument registration is a no-op, and an empty registry returns an empty array. |
| `packages/ai/src/text-stream/to-text-stream.test.ts` | `AiTextStreamUpstreamTests.swift`, `CoreUtilities.swift` | Covered as Swift `toTextStream(_:)`: converts a `LanguageStreamPart` async stream into text chunks by forwarding only text delta parts in order and ignoring start/end/lifecycle parts. |
| `packages/ai/src/generate-text/validate-tool-approvals.test.ts` | `AiValidateToolApprovalsUpstreamTests.swift`, `ToolPreparation.swift`, `Errors.swift` | Covered as Swift `validateApprovedToolApprovals(...)`: approved tool approvals are kept when the reconstructed input matches the executable tool schema; invalid types and forged strict-schema properties throw `AIInvalidToolInputError`; server-side approval policy is re-run against the approved input and can move approvals to denied while preserving or replacing denial reasons; HMAC signatures pass, missing/invalid signatures throw `AIInvalidToolApprovalSignatureError`, and signatures are ignored when no secret is configured. Upstream's client-side tool-without-execute passthrough is API-shape-inapplicable because Swift `AITool` is always executable. Integration of collected approved approvals into full `generateText` / `streamText` resume flows is covered by the adjacent facade slices. |
| `packages/ai/src/generate-text/validate-tool-context.test.ts` | `AiValidateToolContextUpstreamTests.swift`, `ToolPreparation.swift` | Covered as Swift `validateToolContext(toolName:context:contextSchema:)`: missing schema returns the original JSON value, matching schema returns the validated value, and schema failures throw `AITypeValidationError` with upstream-equivalent `field: "tool context"` and tool entity name metadata. |
| `packages/ai/src/embed/embed.test.ts` | `AiEmbedUpstreamTests.swift`, existing `AIFacadeMediaEmbeddingAndUploadTests.swift`, existing telemetry/warning tests | Covered through Swift `AI.embed(...)`: single-value embedding returns embeddings, usage, warnings, provider metadata, response metadata, synthesized request metadata, and forwards dimensions, headers, provider options, and extra body to the model. JS `onStart` / `onEnd` callback event shapes, deprecated `experimental_telemetry`, and callback-throws behavior are API-shape gaps because Swift exposes typed telemetry integrations rather than per-call embed callback options. |
| `packages/ai/src/embed/embed-many.test.ts` | `AIFacadeMediaEmbeddingAndUploadTests.swift`, `AIFacadeMedia.swift` | Covered through Swift `AI.embedMany(...)`: multi-value embeddings are chunked by explicit `chunkSize`, model requests preserve dimensions/provider options, embeddings and warnings are aggregated, token usage is summed, raw chunk values are retained, provider metadata is merged, response metadata is preserved, and telemetry records `ai.embedMany` start/end. JS model-owned `maxEmbeddingsPerCall`, `supportsParallelCalls`, and `maxParallelCalls` scheduler branches are API-shape gaps for the current Swift facade, which uses explicit serial chunking instead. |
| `packages/ai/src/generate-image/generate-image.test.ts` | `AiGenerateImageUpstreamTests.swift`, existing `AIFacadeMediaEmbeddingAndUploadTests.swift`, provider image tests | Initial portable slice covered through Swift `AI.generateImage(...)`: prompt/files/mask/size/aspect ratio/seed/count/provider options/extra body/headers/abort signal are forwarded to the image model; returned URLs/base64 images, warnings, usage, provider metadata, response metadata, and synthesized request metadata surface on the result; empty image output throws `AINoOutputError` with response metadata. Provider-specific image data URI/base64 conversion and media request serialization are covered by provider tests and `ImageModelFileDataURITests.swift`. JS `onStart` / `onEnd` callbacks, user-agent injection at facade boundary, result wrapper `image/images` byte-array convenience accessors, and model-owned `maxImagesPerCall` multi-call batching are API-shape gaps for the current Swift facade unless it later adds those public surfaces. |
| `packages/ai/src/generate-object/generate-object.test.ts` | `AiGenerateObjectUpstreamTests.swift`, existing `AIObjectFacadeTests.swift`, existing `AIObjectFacadeGenerationAndStreamingTests.swift`, existing `AIObjectFacadeOutputStrategyTests.swift` | Covered through Swift `AI.generateObject(...)`, `AI.generateObjectArray(...)`, `AI.generateEnum(...)`, and `AI.generateJSON(...)`: object output decodes typed/raw objects and forwards prompt, headers, provider options, JSON response format schema/name/description, and custom JSON Schema; result warnings, warning logging including empty arrays, request metadata, response metadata, provider metadata, reasoning, finish reason, and usage are preserved; repair covers parse errors, schema validation errors, nil repair, and failed repair; schema-validation, parse, repaired-parse, and empty-text failures throw `AIObjectGenerationError` with response metadata, usage, and finish reason; array, enum, and no-schema output modes produce upstream-equivalent objects and request response formats. Existing callback tests cover typed `AIObjectGenerationCallbacks` order, shared call IDs, start/step/finish metadata, reasoning, provider metadata, response metadata, and error callbacks. JS zod transform/preprocess runtime schema effects, `result.toJsonResponse`, `supportedUrls()` dynamic `this`, `_internal.generateId`, deprecated `experimental_telemetry`, user-agent injection at facade boundary, and throwing callback behavior are API-shape gaps for Swift's typed Codable/JSONSchema, no HTTP `Response` helper layer, `LanguageModel.supportedURLs` property, UUID-owned call IDs, single telemetry surface, provider serializers, and non-throwing callback closures. |
| `packages/ai/src/generate-object/inject-json-instruction.test.ts` | `ProviderUtilsUpstreamParityTests.swift`, `AIObjectParsing.swift` | Covered as Swift `jsonInstructionText(...)` / `injectJSONInstruction(...)`: prompt+schema, prompt-only, schema-only, no prompt/no schema, custom prefix/suffix, empty prompt, empty object schema, structured/nested/special-character/long schemas, injecting into existing/missing/empty system messages, and no-schema/custom suffix message cases. JS `null as any` option inputs are API-shape-inapplicable to Swift typed optionals; dictionary-heavy schema snapshots are compared structurally because Swift JSON object key order is not the JS insertion-order contract. |
| `packages/ai/src/generate-object/stream-object.test.ts` | `AiStreamObjectCoreUpstreamTests.swift, AiStreamObjectVariantsUpstreamTests.swift, AiStreamObjectRepairUpstreamTests.swift, AiStreamObjectWarningsUpstreamTests.swift, AiStreamObjectCallbacksUpstreamTests.swift`, existing `AIObjectFacadeTests.swift`, existing `AIObjectFacadeGenerationAndStreamingTests.swift`, existing `AIObjectFacadeOutputStrategyTests.swift` | Covered through Swift `AI.streamObject(...)`, `AI.streamObjectArray(...)`, `AI.streamEnum(...)`, and `AI.streamJSON(...)`: object/text/full stream equivalents emit upstream-style object deltas and text deltas; schema/name/description, headers, provider options, request messages, usage, finish reason, provider metadata, response metadata, raw object, and final typed object are preserved; invalid schema, parse failure, and no-text cases throw `AIObjectGenerationError` with completed call metadata; array output streams complete-element partial arrays for multi-chunk and single-chunk input; enum output suppresses invalid values, exposes ambiguous raw prefixes, resolves non-ambiguous prefixes, and deduplicates repeated resolved partials; no-schema JSON uses JSON response format without schema/name/description; `repairText` covers parse repair, schema repair, nil repair, markdown code fences, and failed repair metadata; warnings surface on result/stream and log at the object-stream layer; callbacks fire in upstream order, correlate call IDs, expose start/step/finish metadata, return valid final object data, and invoke `onError` for model stream failures. JS-only `result.objectStream`/`textStream`/`fullStream` separate handles, Web/Node response helpers, `_internal.generateId`, deprecated `experimental_telemetry`, throwing callbacks, `supportedUrls()` dynamic `this`, and partial-stream error suppression semantics are API-shape gaps for Swift's direct `AsyncThrowingStream`, typed callbacks, UUID-owned call IDs, and `LanguageModel.supportedURLs` property; related supported URL/media behavior is covered by provider tests. |
| `packages/ai/src/generate-speech/generate-speech.test.ts` | `AiGenerateSpeechUpstreamTests.swift`, existing `AIFacadeMediaEmbeddingAndUploadTests.swift` | Covered through Swift `AI.generateSpeech(...)`: text, voice, output format, speed, language, instructions, provider options, headers, and abort signal are forwarded to the speech model; audio bytes, warnings, provider metadata, request metadata, and response metadata are returned; warning logging fires for both non-empty and empty warning arrays; empty audio throws `AINoOutputError(kind: .speech)` with response metadata, including response headers. JS facade-level `user-agent` injection is an API-shape gap for Swift because user-agent/version headers are provider serializer responsibilities and are covered by provider-specific speech tests. |
| `packages/ai/src/generate-video/generate-video.test.ts` | `AiGenerateVideoUpstreamTests.swift`, existing `AIFacadeMediaEmbeddingAndUploadTests.swift` | Covered through Swift `AI.generateVideo(...)`: prompt, aspect ratio, duration, image, resolution, FPS, seed, count, provider options, headers, and abort signal are forwarded to the video model; URL and base64 video outputs, warnings, provider metadata, request metadata, and response metadata are returned; non-empty warnings are logged and empty warning arrays are not logged for video; empty video output throws `AINoOutputError(kind: .video)` with response metadata, including response headers; upstream `frameImages` and `inputReferences` call options are represented on `VideoGenerationRequest`, including first-frame image normalization, last-frame-only behavior, and warnings when prompt image/input references are superseded by frame images. JS facade-level `user-agent` injection, `result.video` wrapper media-type/Uint8Array helpers, facade-side URL download/media sniffing, multi-call `maxVideosPerCall` splitting/metadata aggregation, and union object-prompt normalization are API-shape gaps for Swift's typed `VideoGenerationRequest`, provider-owned downloading/serializers, `urls`/`base64Videos` result storage, and current `VideoModel` protocol. Provider-specific serializer coverage for `frameImages`/`inputReferences` is ported for Alibaba, ByteDance, Gateway, Google Generative AI, Google Vertex, KlingAI, and xAI video models. Fal, Prodia, and Replicate upstream video tests only include `frameImages: undefined` / `inputReferences: undefined` in default option objects and have no additional serializer assertions for these fields, so there is no Swift-specific provider behavior left to port from this video diff. |
| `packages/ai/src/rerank/rerank.test.ts` | `AiRerankUpstreamTests.swift`, existing `AIFacadeMediaEmbeddingAndUploadTests.swift` | Covered through Swift `AI.rerank(...)`: string and object document requests forward query, documents, topK, provider options, headers, and abort signal; ranking, string document payloads, provider metadata, request metadata, response metadata/body/headers, raw value, warning logging, and telemetry start/end events are preserved on Swift result/telemetry surfaces. JS `RerankResult<T>` generic `originalDocuments`/`rerankedDocuments` wrappers for object documents, `onStart`/`onEnd` callbacks, `_internal.generateCallId`, callback-ordering/throwing behavior, and deprecated `experimental_telemetry` alias are API-shape gaps for Swift's direct `RerankingResult`, string-only `RerankedDocument.document`, UUID-owned telemetry call IDs, and single telemetry options surface. |
| `packages/ai/src/transcribe/transcribe.test.ts` | `AiTranscribeUpstreamTests.swift`, existing `AIFacadeMediaEmbeddingAndUploadTests.swift` | Covered through Swift `AI.transcribe(...)`: audio bytes, file name, MIME type, language, prompt, provider options, headers, and abort signal are forwarded to the transcription model; transcript text, segments, language, duration, warnings, provider metadata, request metadata, and response metadata are returned; warning logging fires for both non-empty and empty warning arrays; empty transcript throws `AINoOutputError(kind: .transcript)` with response metadata, including response headers. JS facade-level `user-agent` injection is an API-shape gap for Swift because user-agent/version headers are provider serializer responsibilities and are covered by provider-specific transcription tests. |
| `packages/ai/src/util/cosine-similarity.test.ts` | `AiCoreUtilityUpstreamTests.swift`, existing `CoreContractTests.swift` | Covers positive, negative, length mismatch, zero-vector, and very-small-magnitude cases. |
| `packages/ai/src/util/fix-json.test.ts` | `AiPartialJSONUpstreamTests.swift` | Covered as Swift `fixJson(_:)` for empty input, literals, numbers, strings, incomplete escapes/unicode escapes, arrays, objects, nesting, and upstream regression cases. Local scanner now mirrors upstream unicode escape state handling. |
| `packages/ai/src/util/get-potential-start-index.test.ts` | `AiUtilityDataUpstreamTests.swift` | Covered as Swift `getPotentialStartIndex(_:_:)`, including full match, empty search, missing search, and partial suffix/prefix overlap cases. |
| `packages/ai/src/util/is-deep-equal-data.test.ts` | `AiUtilityDataUpstreamTests.swift` | Covered for Swift `JSONValue` data: primitives, different types, null/object mismatch, objects, nested objects, arrays, and array-vs-object distinction. JS prototype/date/function cases are runtime-inapplicable. |
| `packages/ai/src/util/async-iterable-stream.test.ts` | `AiAsyncIterableStreamUpstreamTests.swift` | Covered as Swift `createAsyncIterableStream` over `AsyncThrowingStream`, including non-empty and empty async iteration, iterator reads, normal completion without cancellation, and source error propagation. JS Web `ReadableStream` async-iterator `return()` / `throw()` / explicit `cancel()` hooks are runtime-inapplicable unless we introduce a custom stream runtime instead of Swift `AsyncThrowingStream`. |
| `packages/ai/src/util/create-stitchable-stream.test.ts` | `AiStitchableStreamUpstreamTests.swift` | Covered as Swift `createStitchableStream`, including immediate close, one/two/three inner streams, empty inner streams, reads pending before add, pending collection across later streams, inner stream error propagation, add-after-close/terminate errors, and terminate closing before later chunks. JS `ReadableStreamDefaultReader.cancel()` flags for queued inner streams are runtime-inapplicable with native `AsyncThrowingStream` unless we add a custom cancellable stream runtime. |
| `packages/ai/src/util/download/download.test.ts` | `AiDownloadUpstreamTests.swift`, existing `DownloadURLValidationTests.swift`, existing `HTTPResponseSizeLimitTests.swift` | Covered through Swift `downloadURL`, `validateDownloadURL`, and `readResponseWithSizeLimit`: SSRF host/IP blocking, redirect validation before next hop, safe redirects, redirect limit, successful bytes/media type, inline `data:` URLs, default/custom max size propagation, content-length and streamed size enforcement, and abort-signal forwarding. JS fetch body-cancel/socket-leak assertions and browser opaque redirects are runtime-inapplicable to the Swift `AITransport` abstraction. |
| `packages/ai/src/logger/log-warnings.test.ts` | `WarningLoggingTests.swift` | Covered for Swift `AIWarningLogging`: disabled logging scope, custom logger events, empty warning arrays ignored, multiple warnings forwarded as one event, facade/stream warning emission, and upstream warning message formatting for unsupported/deprecated/other warnings with provider/model scope. Node `process.emitWarning` and one-time `console.info` advisory mechanics are runtime-inapplicable to the Swift logger abstraction. |
| `packages/ai/src/middleware/add-tool-input-examples-middleware.test.ts` | `AiToolInputExamplesMiddlewareUpstreamTests.swift`, existing `SpecializedMiddlewareTests.swift` | Covered as Swift `addToolInputExamplesMiddleware` / `toolWithInputExamplesInDescription`, including appending examples to existing or missing descriptions, custom prefix, custom formatter, default removal, `remove: false`, passthrough without examples, empty examples, provider tools without examples, mixed multiple tools, and empty tool dictionaries. Swift tools are keyed dictionaries, so JS array ordering snapshots map to per-key semantic assertions. |
| `packages/ai/src/middleware/default-embedding-settings-middleware.test.ts` | `AiDefaultSettingsMiddlewareUpstreamTests.swift`, existing `SpecializedMiddlewareTests.swift` | Covered as Swift `defaultEmbeddingSettingsMiddleware` / `applyingDefaultEmbeddingSettings`, including header merging, request precedence, empty default/request/both-empty cases, provider option propagation, and both-empty provider options. Swift dictionaries use empty dictionaries where upstream JS distinguishes omitted/`undefined`. |
| `packages/ai/src/middleware/default-settings-middleware.test.ts` | `AiDefaultSettingsMiddlewareUpstreamTests.swift`, existing `MiddlewareTests.swift` | Covered as Swift `defaultSettingsMiddleware` / `applyingDefaultSettings`, including default scalar settings, request precedence, `temperature == 0`, stop sequences, topP/maxOutputTokens, deep provider option merging, headers merging, empty default/request/both-empty cases, and provider options empty cases. JS `null` option override is runtime-inapplicable because Swift typed optionals cannot represent null distinct from missing. |
| `packages/ai/src/middleware/extract-json-middleware.test.ts` | `AiExtractJsonMiddlewareUpstreamTests.swift`, existing `SpecializedMiddlewareTests.swift` | Covered as Swift `extractJsonMiddleware` / `defaultExtractJSONTransform` / `transformTextStream`, including generate and stream fence stripping, untagged fences, no-fence content, custom transform, preserving non-text result content, split fences, character-by-character deltas, large content, extra whitespace, empty fenced content, plain/backtick content, non-text stream chunks, multiple text IDs, text deltas without prior text-start, and short prefix flushing. JS `Object.prototype` pollution assertions are runtime-inapplicable to Swift dictionaries, but `__proto__` text IDs are covered as ordinary keys. |
| `packages/ai/src/middleware/extract-reasoning-middleware.test.ts` | `AiExtractReasoningMiddlewareUpstreamTests.swift`, existing `SpecializedMiddlewareTests.swift` | Covered as Swift `extractReasoningMiddleware` / `extractTaggedSections` / `extractReasoningStream`, including generated reasoning extraction, no-text reasoning, multiple tags, `startWithReasoning`, stream split tags, multiple interleaved reasoning/text segments, empty tags, no-tag passthrough, and `__proto__` text IDs as ordinary dictionary keys. JS `Object.prototype` pollution assertions are runtime-inapplicable to Swift dictionaries. |
| `packages/ai/src/middleware/simulate-streaming-middleware.test.ts` | `AiSimulateStreamingMiddlewareUpstreamTests.swift`, existing `SpecializedMiddlewareTests.swift` | Covered as Swift `simulateStreamingMiddleware`, including generate-backed streaming, no underlying stream call, warnings, text, reasoning, empty text, tool calls, tool results, tool approval requests/responses, finish usage, and provider metadata preservation via `.finishMetadata`. Upstream JS content-array ordering variants for multiple reasoning objects map to Swift's flat `TextGenerationResult.reasoning` plus ordered text/tool/source arrays. |
| `packages/ai/src/middleware/wrap-embedding-model.test.ts` | `AiWrapModelMiddlewareUpstreamTests.swift`, existing `MiddlewareTests.swift` | Covered as Swift `wrapEmbeddingModel`, including provider/model ID passthrough and overrides, transform request sequencing, wrap embed ordering, middleware array immutability, and provider-level embedding middleware. JS `maxEmbeddingsPerCall` and `supportsParallelCalls` wrapper properties are runtime-inapplicable because Swift `EmbeddingModel` protocol does not expose those properties. |
| `packages/ai/src/middleware/wrap-image-model.test.ts` | `AiWrapModelMiddlewareUpstreamTests.swift`, existing `MiddlewareTests.swift` | Covered as Swift `wrapImageModel`, including provider/model ID passthrough and overrides, transform request sequencing, wrap generate ordering, middleware array immutability, and provider-level image middleware. JS function-valued `maxImagesPerCall` / `this` binding assertions are runtime-inapplicable because Swift `ImageModel` protocol does not expose that property. |
| `packages/ai/src/middleware/wrap-language-model.test.ts` | `AiWrapModelMiddlewareUpstreamTests.swift`, existing `MiddlewareTests.swift` | Covered as Swift `wrapLanguageModel`, including provider/model ID passthrough and overrides, supported URL passthrough, generate/stream transform request sequencing, wrap generate/stream ordering, middleware array immutability, and multiple middleware order. Swift has no middleware-level `overrideSupportedUrls`; wrapper now preserves the model's `supportedURLs`. |
| `packages/ai/src/middleware/wrap-provider.test.ts` | `AiWrapModelMiddlewareUpstreamTests.swift`, existing `MiddlewareTests.swift`, existing `CustomProviderTests.swift` | Covered as Swift `wrapProvider`, including language/image/embedding middleware application to each requested model, provider-level middleware through registry/custom provider paths, and unsupported file/skill routing. Upstream JS V2 provider adapter and `process.emitWarning` assertions are runtime-inapplicable because the Swift port exposes one current protocol shape and explicit typed errors. |
| `packages/ai/src/model/resolve-model.test.ts` | `AIModelResolutionTests.swift`, existing `ProviderRegistryVercelTests.swift` | Covered as Swift `AI.resolve*Model` and string-model facade resolution, including explicit provider resolution for language, embedding, image, transcription, speech, video, and reranking models; default provider scoped resolution for language/embedding/image facade calls; string model errors as failing streams; and gateway provider family aliases. JS object-or-string overloads, global `AI_SDK_DEFAULT_PROVIDER`, and V2/V3-to-V4 adapter branches map to Swift's explicit provider argument, `AIDefaultProvider`, and current-protocol model surfaces. |
| `packages/ai/src/prompt/create-tool-model-output.test.ts` | `AiPromptToolOutputUpstreamTests.swift`, existing `AIFacadeTests.swift`, existing `MCPClientTests.swift` | Covered through Swift tool execution: `AITool.toModelOutput` receives tool call ID, parsed input, and raw output; custom text/json/content-shaped outputs are preserved on `AIToolResult.modelOutput`; MCP tool output conversion covers content/file fallback; raw JSON tool results are preserved when no `toModelOutput` hook is supplied. JS default `ToolResultOutput` wrapper synthesis, `undefined`, stringification error modes, and `this` binding checks are runtime-inapplicable to the Swift closure-based tool model. |
| `packages/ai/src/prompt/prepare-language-model-call-options.test.ts` | `AiLanguageModelCallOptionsUpstreamTests.swift`, existing `AIFacadeToolLoopTests.swift` timeout coverage | Covered as Swift `prepareLanguageModelCallOptions(_:)` and facade validation: valid typed language settings pass through to the model, nil optionals are preserved, reasoning values including `provider-default` pass through, and `maxOutputTokens < 1` is rejected before generate/stream model calls. JS invalid runtime-type checks are handled by Swift static types. JS object-based `TimeoutConfiguration` helpers (`totalMs`/`stepMs`/`chunkMs`/`toolMs`) are runtime-inapplicable because the Swift API exposes direct `timeoutNanoseconds`, `AIRetryPolicy.timeoutNanoseconds`, and separate stream/tool-loop controls instead of a shared timeout object. |
| `packages/ai/src/prompt/prepare-tool-choice.test.ts` | `AiPrepareToolChoiceUpstreamTests.swift` | Covered as Swift `prepareToolChoice(_:)`, including missing tool choice defaulting to `{type:auto}`, string choices `none`/`auto`/`required`, and object tool choice preserving `toolName`. The helper is intentionally separate from `LanguageModelRequest.toolChoice` so the existing Swift request surface can continue distinguishing omitted tool choice from explicit auto. |
| `packages/ai/src/prompt/prepare-tools.test.ts` | `AiPrepareToolsUpstreamTests.swift` | Covered as Swift `prepareTools(tools:toolOrder:)` plus `AITool` metadata encoding: missing/empty tools return nil, function tools produce upstream-shaped `{type:function,name,inputSchema}`, provider-defined tools pass through `{type:provider,name,id,args}`, partial `toolOrder` is honored with omitted tools sorted alphabetically, duplicate order entries do not duplicate tools, and `providerOptions`, `strict`, and `inputExamples` are emitted as tool-level metadata while stripped from `inputSchema`. Upstream dynamic description callbacks are represented by already-resolved Swift `AITool.description` values because Swift tool descriptions are typed strings rather than JS callbacks with `toolsContext` / sandbox parameters. |
| `packages/ai/src/prompt/standardize-prompt.test.ts` | `AiStandardizePromptUpstreamTests.swift` | Covered as Swift `standardizePrompt(...)`: missing prompt/messages and conflicting prompt/messages throw typed invalid-argument errors, prompt strings become user messages, empty messages are rejected, system messages in prompt/messages are rejected by default and allowed when configured, system messages with non-text parts are rejected, instructions may be one or more system messages, `system` falls back to instructions, and explicit instructions take precedence over `system`. JS runtime schema validation for arbitrary object-shaped messages is represented by Swift static `AIMessage` typing plus explicit system-message shape checks. |
| `packages/ai/src/prompt/convert-to-language-model-prompt.validation.test.ts` | `AiConvertToLanguageModelPromptValidationUpstreamTests.swift` | Covered as Swift language-model request validation through `prepareLanguageModelCallOptions(_:)`: provider-executed tool calls may defer results, approval responses satisfy matching tool calls, provider-executed approval responses are preserved in messages, and ordinary non-provider-executed tool calls without tool results throw `AIMissingToolResultsError` with the missing tool call IDs. General prompt message conversion is covered by the adjacent `convert-to-language-model-prompt.test.ts` entry. |
| `packages/ai/src/prompt/convert-to-language-model-prompt.test.ts` | `AiConvertToLanguageModelPromptUpstreamTests.swift`, `ResponsesEndpointOpenResponsesAndPerplexityTests.swift`, `AmazonBedrockAnthropicAndConverseStreamingTests.swift`, `PromptConversion.swift`, `OpenAIResponsesInput.swift`, `AnthropicBedrockAdapter.swift`, `prepareLanguageModelCallOptions(_:)` | Covered for Swift typed prompt conversion: system instructions are prepended; empty user/assistant text without provider metadata is filtered; user message provider options are preserved; assistant approval requests and non-provider approval responses are filtered before provider calls while provider-executed approval responses are preserved; consecutive tool messages are combined and empty tool messages are dropped; typed user media/reference parts, assistant reasoning/provider metadata, assistant file/provider-reference parts, assistant inline file data with filename/provider metadata, assistant tool call/result metadata and providerExecuted flags, tool result metadata/error flags, and canonical tool-result `output: { type: "content", value: [{ type: "file", ... }] }` model output are preserved. Provider-level surfaces cover canonical OpenAI Responses tool-result file content plus Bedrock Anthropic URL/data conversion, including data URLs and generic downloaded media type detection. `custom` and `reasoning-file` are now represented by typed `AIContentPart` cases and covered under stream/response-message ports; provider-specific resend support remains API-dependent. JS untyped string/data-url unions, Node `process.emitWarning` deprecation branches for legacy tool-result content unions, and `Object.prototype` collision assertions are runtime-inapplicable to Swift's typed `AIMessage` API. Deprecated legacy tool-result content shapes remain provider-level compatibility behavior where already covered, but are not carried as a new generic prompt API because this port does not need backward compatibility for JS legacy unions. |
| `packages/ai/src/registry/custom-provider.test.ts` | `CustomProviderTests.swift`, `AIModelResolutionTests.swift` | Covered for Swift `customProvider`: configured model/client lookup, fallback provider lookup, local-over-fallback precedence, string model IDs through explicit/default provider resolution, missing-model errors for all model families, and file/skill client behavior. JS V2/V3-to-V4 adapter cases are runtime-inapplicable because the Swift port exposes one current protocol shape per model family. |
| `packages/ai/src/registry/provider-registry.test.ts` | `CustomProviderTests.swift`, `MiddlewareTests.swift` | Covered for Swift `createProviderRegistry`: provider/model ID splitting with extra separators, custom separators, all model families, invalid IDs, missing providers, file/skill routing and unsupported errors, and language/image middleware applied to routed models. JS V3 provider adapter cases are runtime-inapplicable for the same single-protocol Swift runtime reason. |
| `packages/ai/src/upload-file/upload-file.test.ts` | `AIFacadeMediaEmbeddingAndUploadTests.swift`, existing `CustomProviderTests.swift`, existing `FileAndSkillClientTests.swift` | Covered as Swift `AI.uploadFile(client:request:)`, including client delegation, data/media type/filename forwarding, provider options forwarding, warnings, provider metadata, request metadata filling, registry/provider file-client resolution, unsupported provider errors, and provider-specific file option validation. Upstream JS tagged data/base64/text union maps to Swift's `Data` plus explicit `mediaType` request shape. |
| `packages/ai/src/upload-skill/upload-skill.test.ts` | `AIFacadeMediaEmbeddingAndUploadTests.swift`, existing `CustomProviderTests.swift`, existing `FileAndSkillClientTests.swift` | Covered as Swift `AI.uploadSkill(client:request:)`, including client delegation, files/display title forwarding, provider options forwarding, warnings, provider metadata, request metadata filling, registry/provider skills-client resolution, unsupported provider errors, and OpenAI/Anthropic skill client metadata. |
| `packages/ai/src/util/merge-callbacks.test.ts` | `AiCallbackUtilityUpstreamTests.swift` | Covered as Swift `mergeCallbacks(_:)` with optional callbacks, parallel execution, wait-for-all-settle behavior, and ignored thrown/rejected errors. JS microtask-order snapshots are represented as semantic assertions because Swift task scheduling order is runtime-owned. |
| `packages/ai/src/util/merge-abort-signals.test.ts` | `AiMergeAbortSignalsUpstreamTests.swift` | Covered as Swift `mergeAbortSignals`, including nil filtering, direct return for one signal, first abort reason wins, already-aborted signals, many signals, and timeout sources via typed `AIAbortSource.timeoutMilliseconds`. JS arbitrary object reasons map to the existing Swift string reason surface. |
| `packages/ai/src/util/merge-objects.test.ts` | `AiUtilityDataUpstreamTests.swift` | Covered as Swift `mergeObjects(_:_:)` for flat/deep merge, array replacement, null values, empty/nil inputs, immutability, complex nested structures, and top-level/nested dangerous-key filtering. JS `undefined`, `Date`, and `RegExp` cases are runtime-inapplicable. |
| `packages/ai/src/util/notify.test.ts` | `AiCallbackUtilityUpstreamTests.swift` | Covered as Swift `notify(event:callback:)` / `notify(event:callbacks:)` for single, array, nil/omitted callbacks, async waiting, parallel callbacks, swallowed errors, generic event typing, nested event shapes, and repeated calls. JS fake-timer mechanics are runtime-inapplicable. |
| `packages/ai/src/util/parse-partial-json.test.ts` | `AiPartialJSONUpstreamTests.swift` | Covered as Swift `parsePartialJSON(_:)` for nil input, successful parse, repaired parse through `fixJson`, and failed parse. JS module mocking assertions map to direct state/value assertions. |
| `packages/ai/src/util/prepare-headers.test.ts` | `AiHTTPRetryUtilityUpstreamTests.swift` | Covered as Swift `prepareHeaders(_:defaultHeaders:)` for default header insertion, preserving existing content type, nil init, initialized headers, and response-header-equivalent dictionaries. Browser `Headers` / `Response.headers` object shapes are runtime-inapplicable. |
| `packages/ai/src/util/prepare-retries.test.ts` | `AiHTTPRetryUtilityUpstreamTests.swift` | Covered as Swift `prepareRetries(maxRetries:)`, including upstream default `maxRetries == 2`; Swift `Int` excludes the JS non-integer validation branch, and negative values are validated. |
| `packages/ai/src/util/retry-with-exponential-backoff.test.ts` | `AiRetryBackoffUpstreamTests.swift`, existing facade retry tests | Covered through Swift `retryDelayNanoseconds` / `withRetry`, including `retry-after-ms`, `retry-after` seconds/date parsing, invalid and negative header fallback, too-long delay fallback, header precedence, provider-shaped 429 errors, gateway retryability, and non-retryable gateway authentication. Fake timer windows map to direct selected-delay assertions and short real retry checks. |
| `packages/ai/src/util/serial-job-executor.test.ts` | `AiSerialJobExecutorUpstreamTests.swift` | Covered as Swift `AISerialJobExecutor` with immediate enqueue and `Task<Void, Error>` results, including serial order, one-at-a-time execution, thrown errors, mixed success/failure continuation, and concurrent run-call ordering. |
| `packages/ai/src/util/set-abort-timeout.test.ts` | `AiAbortTimeoutUpstreamTests.swift` | Covered as Swift `setAbortTimeout(abortController:label:timeoutMilliseconds:)`, including no early abort, timeout abort, `TimeoutError` reason name, message, cancellation via returned task, nil controller, and nil timeout. Fake timer mechanics map to short real async waits. |
| `packages/ai/src/util/simulate-readable-stream.test.ts` | `AiSimulateReadableStreamUpstreamTests.swift`, existing `CoreContractTests.swift` | Covered as Swift `simulateReadableStream`, including emitted chunks, empty chunks, generic value payloads, initial/chunk delay selection, and nil delay values through an injected delay hook. JS `ReadableStream` reader mechanics map to Swift `AsyncThrowingStream` collection. |
| `packages/ai/src/util/split-array.test.ts` | `AiCoreUtilityUpstreamTests.swift` | Covered as Swift `splitArray(_:chunkSize:)`; non-integer chunk-size flooring is represented at the caller boundary with `Int(floor(...))` because Swift chunk sizes are integers. |

## Pending Agent / UI Response Ports

None currently pending.

## Partial Generate / Stream Text Coverage

- `packages/ai/src/generate-text/generate-text.test.ts`
  - `AiGenerateTextUpstreamTests.swift`, `CoreTextObjectResults.swift`, `ResultContent.swift`, `AIFacadeGenerateText.swift`, `ResponseMessages.swift`
  - Ported first result-surface slice: `result.content` preserves typed text/source/file/reasoning/reasoning-file/custom parts, `result.files` excludes reasoning files, executed tool results are appended to result content, and `result.responseMessages` aggregates assistant/tool messages per step without merging assistant parts across tool-message boundaries.
  - Ported second result-surface slice: `result.reasoningText`, `result.warnings`, `result.sources`, `result.files`, file parts in `result.content`, `result.steps` exposing reasoning/sources/files/content, and `result.finalStep` for the final step-local view.
  - Ported third result-surface slice: refined tool input propagates into `result.toolCalls`, step tool calls, response messages, and subsequent model requests; executed tool results remain exposed on result and step surfaces; provider metadata is exposed on result and final step; response metadata id/timestamp/model ID/headers is exposed on the result and step response surfaces, while response body is omitted by default and retained only with `includeResponseBody`.
  - Ported prepare-step slice: `prepareStep` receives initial request messages, current accumulated request messages, and response messages; prepare-step request overrides are used for the current step; prepared messages and system-message instructions are carried forward into later tool-loop steps together with prior response messages.
  - Ported prepare-step tool-context slice: Swift `LanguageModelRequest.toolContexts` is visible to `prepareStep`, can be replaced for later generate steps, is forwarded to model requests, and reaches tool execution context after schema validation.
  - Ported prepare-step active-tools slice: Swift `AIPrepareStepResult(executableTools:)` now replaces the step tool list, including the upstream-equivalent empty active-tools case.
  - Ported callback-equivalent telemetry slice: tool execution telemetry fires for each tool call, carries parsed arguments and tool results, and records tool execution errors; language-model telemetry wrappers run after step start and before tool execution, and tool telemetry wrappers surround tool execution before step end. Swift `AIToolExecutionContext.messages` carries the same accumulated request/response messages that upstream tool execution callbacks receive across multi-step loops.
  - Ported step-start telemetry slice: Swift `ai.generateText.step` telemetry records the upstream-portable `onStepStart` lifecycle data, including first-step request messages, subsequent-step accumulated user/assistant/tool messages, and provider/model changes applied by `prepareStep`.
  - Audited `onEnd` / deprecated `onFinish` result payload slice: Swift exposes the same portable final generation data through `TextGenerationResult` and `AIToolStep` surfaces already covered here, including text, content, tool calls/results, response messages, steps/finalStep, usage/step usage, warnings, provider metadata, and response metadata.
  - Existing telemetry coverage maps the upstream telemetry-integrations block to Swift's typed telemetry API: per-call integrations receive `start`/`retry`/`end`/`error`/`abort`, multiple per-call integrations record in order, per-call integrations override registered integrations, tool-loop telemetry emits step and tool start/end events, registered integrations require explicit per-call opt-in, and language/tool execution wrappers run in integration order. JS user lifecycle callbacks, automatic global `AI_SDK_TELEMETRY_INTEGRATIONS` discovery, and swallowed throwing listener callbacks are API-shape gaps; Swift exposes `Telemetry.Options` and `Telemetry.register(...)`, `Telemetry.Integration.record` is non-throwing, and `executeLanguageModelCall` / `executeTool` wrappers are part of the typed execution path rather than fire-and-forget listeners.
  - Ported `stopWhen` slice: tool-loop `generateText` can stop after a tool step, stop conditions receive the current accumulated steps in order, `isLoopFinished()` completes the tool loop, `result.usage` sums usage across steps, all-step tool calls/results aggregate on the result, final-step tool calls/results remain step-local, step response metadata is preserved, and `result.finalStep.usage` remains the final step-local usage.
  - Ported `options.output` text/object/array/choice slices: `Output.text()` returns final text as output/rawOutput, sets `responseFormat == .text`, and preserves the prompt; object, array, choice, and JSON outputs send upstream-equivalent JSON response formats/schemas, parse final output/rawOutput, and stream partials where applicable. Swift's default `AI.generateText(...)` already exposes `TextGenerationResult.text` as the equivalent of upstream's default text output surface.
  - Ported `options.output` + tools slice: Swift `AI.generateText(... output: ..., executableTools:)` now applies the output response format before the tool loop, executes tools normally when the model finishes with `tool-calls`, keeps tool calls/results on the underlying text result, and represents upstream's "No output generated" getter behavior as `AIOutputGenerationResult<Output?>.output == nil` without trying to parse the tool-call step as final JSON.
  - Ported option-forwarding slice: `headers`, `providerOptions`, `reasoning` including `provider-default`, and `abortSignal` in tool execution context are forwarded through `generateText`.
  - Ported tool/provider slice: `generateText` invokes `onInputAvailable` for parsed tool input before execution, provider-executed tool calls/results stay in content/result surfaces, and provider-executed tool calls are not executed locally.
  - Ported tool-error slice: tool execution errors in `generateText` return an error `AIToolResult` with `error-text` output, append it to result content/tool results/response messages, and still emit tool error telemetry. Swift keeps the low-level `executeToolCalls(...)` default throwing behavior for direct helper and stream tests.
  - Ported programmatic-tool-calling slice: mixed provider-executed and client tool calls can occur in one step, provider-defined tools and executable tool metadata are forwarded to the model request, provider-executed calls without local executable tool definitions are forwarded, client tool results are added to subsequent model requests, provider metadata can be used by `prepareStep` to forward provider options, late provider-executed tool results remain assistant response-message content, and accumulated steps/messages are visible to `prepareStep`.
  - Existing facade coverage in `AIFacadeToolLoopTests.swift` covers the adjacent dynamic-tools block: dynamic tool calls/results are annotated on generate result surfaces and subsequent model messages.
  - Existing facade and helper coverage covers the adjacent context block: `toolsContext` / `contextSchema` reaches tool execution, approval, and stream callbacks; `prepareStep` receives accumulated messages/steps. JS `sandbox`, generic `runtimeContext`, and telemetry include allow-lists for runtime/tools context are API-shape gaps in Swift's typed facade.
  - Ported invalid-tool-call slice: invalid model tool input, unknown tools, and argument refinement failures in `generateText` return error `AIToolResult` / response messages instead of escaping from the facade, while lower-level parse/execute helpers still throw typed errors.
  - Ported approval-request slice: `generateText` stops after pending user approval, returns approval requests in content/result/response-message surfaces, invokes generic and tool-defined approval callbacks with parsed input, tool call ID, request messages, and tool context for each tool call, still executes not-applicable tool calls in the same step before appending pending approval requests, executes one or more already-approved tool calls from message history before the next model call / `prepareStep`, passes historical and automatic approval execution context with tool call ID, messages, and abort signal to the tool body, rejects forged approved inputs that fail schema validation, converts server-side policy denial of a client-approved call into an `execution-denied` tool result without running the tool, validates signed historical approvals when `toolApprovalSecret` is configured, serializes approved tool execution failures as `error-text` results, turns denied approval responses into `execution-denied` continuation results, mirrors automatic approval/denial behavior, and preserves provider-executed MCP-style approval flows: provider prompts receive only tool-call/tool-result content for local tools, preserve provider-executed approval responses, and public response messages retain automatic approval request/response metadata.
  - API-shape gaps noted: JS class-based tool `this` binding is runtime-inapplicable because Swift `AITool` stores escaping closures instead of dynamically-bound object methods; JS `include` / `experimental_include` request-body/request-message retention result surfaces, `timeout` object forms, step `performance` result snapshots, local tool `async *execute` preliminary outputs, top-level `activeTools` / `toolOrder` facade options, `experimental_download` generic URL download hooks including the prepareStep model-switch `supportedUrls` download decision, and `onStart` / `onStepStart` / `onStepEnd` / `onStepFinish` / `onEnd` / `onFinish` / `onLanguageModelCallStart` / `onLanguageModelCallEnd` / `onToolExecutionStart` / `onToolExecutionEnd` callbacks do not have a direct Swift `generateText` surface. Swift covers related lifecycle observability through telemetry and `prepareStep`; lower-level tool filtering/order helpers, provider-adapter URL downloading, `supportedURLs` matching, primitive generate timeout through `AIRetryPolicy.timeoutNanoseconds`, retry-policy timeout behavior, provider-emitted preliminary tool results, response-body retention through `includeResponseBody`, and tool result content are covered separately.
  - Runtime changes: `TextGenerationResult` now exposes typed `AIResultContentPart` content, files, response messages, `reasoningText`, and `finalStep`; `AIToolStep` now carries step-local content/files/sources/warnings; tool-loop `generateText` aggregates content/response messages/warnings/usage across steps; refined tool input now rewrites generated tool call arguments in generate mode as stream mode already did; generate-mode tool execution now calls `onInputAvailable`; generate-mode tool execution converts tool body failures and invalid model tool calls to error tool results; pending approval requests are appended after generated tool results while automatic approval request/response pairs stay before their synthetic results; approved tool-approval responses from message history are validated/executed before `prepareStep` and forwarded as tool results to the next model call, while denied historical approvals are forwarded only as `execution-denied` tool results; provider-executed historical approvals bypass local validation/execution, with denied responses adding a provider-executed `execution-denied` result for continuation prompts; `generateText` tool-loop overloads now accept `toolApprovalSecret` for signed historical approval validation; output tool-loop overloads now return optional final output so tool-call-only finishes can expose tool calls/results without parsing missing JSON; `AIPrepareStepResult(executableTools:)` now replaces, rather than merges into, step tools; `AIToolResult` now carries `providerExecuted` for provider results that arrive without their matching call in the same content batch; provider-executed tool calls no longer require local executable tool definitions during forwarding; `toResponseMessages` avoids synthesizing duplicate `execution-denied` results when an explicit denied tool result is already present.
- `packages/ai/src/generate-text/stream-text.test.ts`
  - `AiOutputUpstreamTests.swift`, `Output.swift`, existing `AIFacadeStreamTextCoreTests.swift, AIFacadeStreamTextToolExecutionTests.swift, AIFacadeStreamTextToolLoopAggregationTests.swift`, existing `AiStreamLanguageModelCallUpstreamTests.swift`
  - Ported telemetry model-call context slice: `AI.streamText` executes `LanguageModel.stream(_:)` inside the typed `Telemetry.Integration.executeLanguageModelCall` wrapper and exposes the same call ID to the wrapper and model stream creation path.
  - Ported telemetry integrations slice: multiple per-call `Telemetry.Integration` values on `AI.streamText` receive the upstream-equivalent start event in declaration order.
  - Ported initial `result.textStream` slice through Swift's closest text-only stream projection, `AI.streamText(..., output: Output.text())`: text deltas are emitted in order, empty text deltas are filtered from `.textDelta` and `.partialOutput`, reasoning deltas do not enter the text stream/output text, and final output still carries accumulated reasoning.
  - Ported object-output stream slice through `AI.streamText(..., output: Output.object(...))`: object text deltas now emit upstream-style valid partial text fragments by holding a dangling object-key colon until the first value fragment, partial outputs update as JSON becomes parseable, and final output/text/rawOutput match the completed object.
  - Ported array/choice output stream slices through `Output.array(...)` and `Output.choice(...)`: array partial outputs now include each newly complete element and handle a whole array in one chunk, choice partial outputs deduplicate repeated resolved prefixes, and final output/rawOutput match the parsed array or choice value. Swift currently keeps array/choice `AIOutputGenerationResult.text` as the parsed-output text rather than upstream JS's raw model envelope text; this semantic difference is tracked separately from the portable partial/output behavior.
  - Classified top-level `options.activeTools` / `options.toolOrder` stream tests as API-shape gaps for the current Swift facade: Swift exposes active tool filtering and tool ordering through lower-level `filterActiveTools(...)`, `prepareTools(... toolOrder:)`, and `prepareStep` tool replacement rather than public `AI.streamText(... activeTools/toolOrder:)` parameters.
  - Ported raw-chunks forwarding slice through Swift's public `includeRawChunks` option: `AI.streamText` forwards default/false/true values to the model request and surfaces raw stream parts when the provider emits them. JS `include.rawChunks`, deprecated `includeRawChunks`, and `experimental_include` precedence variants map to Swift's single typed `includeRawChunks` parameter, while `onChunk` raw callback snapshots remain covered by generic stream/telemetry surfaces rather than a JS result-object callback.
  - Ported mixed multi-content streaming slice: Swift stream tool steps now preserve interleaved text/reasoning content blocks by stream part ID and start order, matching upstream `result.content` / `result.steps` content behavior through `prepareStep`-observable step content.
  - Existing full-stream coverage maps adjacent `result.stream` / deprecated `fullStream` checks to Swift's public `AI.streamText(...) -> AsyncThrowingStream<LanguageStreamPart, Error>` surface, including text/reasoning/file/custom/source/metadata forwarding, telemetry aggregation, retries, cancellation, tool loops, approval requests, and provider-executed stream parts.
  - Ported delayed async tool-result slice: `AI.streamText` waits for an asynchronous local tool result before starting the continuation model stream, yields the tool result before later text deltas, and forwards that result in the next prompt.
  - Ported tool-input metadata slice: generic `AI.streamText` preserves provider metadata on streamed tool-input lifecycle parts while forwarding tool input deltas/end and finish usage.
  - Ported error-handling slices: provider stream failures before the first part throw from Swift's `AsyncThrowingStream` and record `ai.streamText` error telemetry; `Output.text()` forwards provider error chunks as raw stream parts and still emits final output/finish when a subsequent finish chunk arrives, preserving accumulated text, finish reason, and usage. JS `Object.prototype` pollution assertions for missing text/reasoning part IDs are runtime-inapplicable to Swift's typed enum/dictionary stream state; Swift treats `"__proto__"` as an ordinary string key where stream state maps are used.
  - Ported retry slice: `AI.streamText` retries retryable start failures, streams the successful retry response, records retry telemetry, does not retry after a part has already been yielded, and preserves request messages including system messages across retry attempts.
  - Ported prepareStep tools-context slice: Swift `AI.streamText` lets `prepareStep` replace request `toolContexts`, forwards the updated contexts to subsequent model stream calls, and passes the updated per-tool context into tool execution.
  - Ported warning result slices on Swift's matching surfaces: `Output.text()` exposes single-step stream-start warnings on the final `AIOutputGenerationResult`, while public `AI.streamText(... executableTools:)` yields warning stream-start parts from every tool-loop step. Multi-step aggregate `warnings` / `finalStep.warnings` result-object snapshots are represented by `AiGenerateTextUpstreamTests.swift` because Swift `streamText` does not expose JS-style promise-backed aggregate result handles.
  - Ported response metadata slice through `Output.text()`: response id, model id, timestamp, and headers emitted by a stream response-metadata chunk surface both as a stream part and on the final `AIOutputGenerationResult`.
  - Ported source/file result slices on Swift's matching surfaces: `Output.text()` exposes single-step sources/files through typed stream parts, raw file parts, and final `AIOutputGenerationResult` content/files; public `AI.streamText(... executableTools:)` yields source/file parts from every tool-loop step; multi-step aggregate `result.sources` / `result.files` / `finalStep` result-object snapshots are represented by `AiGenerateTextUpstreamTests.swift` because Swift `streamText` does not expose JS-style promise-backed aggregate result handles.
  - Ported callback-equivalent tool telemetry slice: Swift `AI.streamText` emits `ai.streamText.tool` start/end events for each tool call in a multi-tool step with parsed arguments and tool results, and emits tool error telemetry when tool execution throws.
  - Ported `stopWhen` step-input slice: stream tool loops now build the next model request from the full synthesized response messages, preserving reasoning content and tool result messages from prior steps.
  - Ported prepare-step active-tools slice: Swift `AIPrepareStepResult(executableTools:)` now replaces the step tool list, including the upstream-equivalent empty active-tools case for later stream steps.
  - Ported multiple stop-conditions slice: Swift `AI.streamText` evaluates stop conditions in order after a completed tool step, passes a step snapshot containing the executed tool result to every condition, and stops the loop when a later condition returns true without making another model stream call. Public `AI.streamText(... executableTools:)` also covers `AIStopCondition.isLoopFinished()` as the upstream sentinel that lets the tool loop continue through the local tool result and finish naturally on the following `stop` step.
  - Classified `options.transform` / `experimental_transform` as an API-shape gap for the current Swift `AI.streamText` surface: upstream JS can post-process synthetic wrapper `TextStreamPart` chunks, mutate finish usage/reason/provider metadata, streamed tool-call input, and local tool-result output, compose multiple transforms, stop the wrapper stream, and pass transformed snapshots into `onChunk` / `onFinish` / `onStepFinish` callbacks. Swift currently exposes request/model middleware and narrower text-stream transforms, but no public post-tool generic `LanguageStreamPart` transform hook at the same lifecycle point.
  - Ported `options.headers` stream slice: Swift `AI.streamText` forwards custom request headers into the model stream request while preserving text stream output.
  - Ported provider-executed tool stream slices: public `AI.streamText` forwards provider-executed tool-input lifecycle parts, provider tool calls, successful provider tool results with metadata, and structured provider tool error results without local execution; stream tool loops also preserve provider-executed tool results and provider metadata in ordered step content, so the next model request replays provider tool call/result content before local tool-call result messages.
  - Ported `options.providerMetadata` / `options.reasoning` stream slices: Swift `AI.streamText` forwards provider options plus ordinary and `provider-default` reasoning settings into the model stream request.
  - Existing `AIFacadeToolLoopTests.swift` coverage maps the adjacent dynamic-tools stream block: dynamic tool calls/results are annotated on streamed input lifecycle parts, public stream parts, and subsequent model messages.
  - Ported `options.abortSignal` stream tool-execution slice: Swift `AI.streamText` passes the request abort signal into contextual tool execution along with the tool call ID and current messages.
  - Existing timeout coverage maps portable stream timeout failure to `AI.streamText(... timeoutNanoseconds:)`; upstream JS timeout object variants (`totalMs`, `stepMs`, `chunkMs`) and merged per-step/chunk `AbortSignal` identity assertions are API-shape gaps because Swift exposes a direct timeout duration plus explicit abort-signal primitives instead of a public timeout object.
  - Ported `tool callbacks` stream slice on the public `AI.streamText` surface: callbacks fire in upstream order for `toolInputStart`, each streamed input delta, and final parsed input availability; stream tool execution no longer emits a duplicate `onInputAvailable` after the forwarding pass has already emitted it.
  - Ported `tools with custom schema` stream slice: Swift `AI.streamText` forwards plain JSON-schema tools, required tool choice, and prompt messages to the model request, and `prepareTools` maps the request into the upstream function-tool shape.
  - Classified `options.messages` `supportedUrls` `this`-binding test as API-shape-inapplicable: Swift exposes `LanguageModel.supportedURLs` as a typed property instead of a JavaScript method with dynamic `this`; prompt/provider URL media handling and supported-URL matching are covered separately.
  - Ported `tool execution errors` stream slice: Swift `AI.streamText` now returns thrown tool body failures as error `AIToolResult` parts with `error-text` output, while retaining tool error telemetry; higher-level JS result/step/UI-message promise surfaces have no direct Swift stream-result object equivalent.
  - Ported `invalid tool calls` stream slice: invalid model tool input in `AI.streamText` is forwarded as the original tool call plus an error `AIToolResult` in the stream and completed step content, without invoking the local tool body. Swift does not currently expose upstream JS's separate invalid tool-call union fields on `AIToolCall`; the portable error-result/content behavior is covered.
  - Classified `tools with preliminary results` as an API-shape gap for current Swift tools: upstream JS local tool `async *execute` can yield preliminary outputs before a final result, while Swift `AITool.execute` / `executeWithContext` currently returns one `JSONValue`. Provider-emitted preliminary `AIToolResult(preliminary: true)` remains supported at the stream part level by provider parsers, but there is no local-tool streaming execute hook to port this block faithfully.
  - Ported provider-executed dynamic tools stream slice: Swift forwarding preserves `dynamic`, `providerExecuted`, and provider metadata on streamed tool-input lifecycle parts, tool calls, tool results, and completed step content.
  - Ported `deferred tool calls with tool-error` stream slice: Swift `AI.streamText` now keeps unresolved provider-executed tool calls pending across step-loop requests, replays them as assistant content, and resolves them when a later provider-executed error `AIToolResult` arrives; same-step provider tool errors still finish in one model call. Swift's provider tool schema is currently represented as `LanguageModelRequest.tools` JSON rather than a typed `supportsDeferredResults` tool option, so the tests cover the portable stream-loop behavior.
  - Ported `logWarnings` multi-step slices: Swift warning logging records one warning event per model step in both `AI.generateText` and `AI.streamText` tool loops, and stream loops avoid an extra aggregate warning log from the outer stream wrapper. Existing `WarningLoggingTests.swift` covers single-step warning logging, multiple warning payloads, formatting, disabled logging, and Swift's deliberate empty-warning no-op behavior.
  - Ported `tool execution approval` stream slices: public Swift `AI.streamText` now has direct coverage that automatic denial does not execute the local tool, emits approval request/response plus `execution-denied` result, continues to the next model step, and replays only tool-call/tool-result content to the model; automatic approval executes the tool, emits approval metadata plus tool result, and continues with the tool result in the next prompt. A mixed user-defined approval callback case verifies that a pending user-approval call stops the loop while a not-applicable call in the same step still executes and emits its result. Historical approval responses in input messages are now executed, serialized to `error-text` when approved tool execution throws, or converted to `execution-denied` before the first model stream call; those results are yielded as stream tool results, passed to `prepareStep.responseMessages`, and replayed in the model prompt. Provider-executed historical approval responses are forwarded in continuation prompts, with denied provider approvals adding provider-executed `execution-denied` results before the model stream. The adjacent UI-message/result-object snapshots remain mapped to Swift chat/UI reducer tests and direct async stream parts.
  - Existing result/step and tool-loop coverage maps adjacent `result.steps`, `result.finalStep`, `result.toolCalls`, and `result.toolResults` checks to Swift `AiGenerateTextUpstreamTests.swift` and `AIFacadeStreamTextCoreTests.swift, AIFacadeStreamTextToolExecutionTests.swift, AIFacadeStreamTextToolLoopAggregationTests.swift`: step-local reasoning/sources/files/content, refined tool calls, executed tool results, dynamic/provider-executed tool behavior, prepareStep accumulated messages/steps, and stream tool-result emission are covered on the Swift public surfaces. Public `AI.streamText(... executableTools:)` now also has an explicit all-step static/dynamic tool-call and tool-result stream-order test; aggregate JS promise result snapshots remain represented by `generateText` result tests because Swift `streamText` does not expose the same result object handles.
  - Existing chunk/finish/response-message coverage maps portable portions of JS `onChunk`, `onEnd` / `onFinish`, and `result.responseMessages` snapshots to Swift public surfaces: `LanguageStreamPart` streaming preserves provider parts in order, `Output.text()` exposes final single-step warnings/usage/finish metadata/response metadata/sources/files, and `AiGenerateTextUpstreamTests.swift` covers final content/tool calls/tool results/provider metadata/response messages on result surfaces.
  - Existing UI-message coverage maps the portable portions of JS `pipeUIMessageStreamToResponse`, `toUIMessageStream`, and response-stream helper tests to Swift `AIUIMessageStreamReducer`, `DirectAIChatTransport`, and `AIChatSession`: text/reasoning/source/file/tool parts build incremental assistant messages, generated response message IDs are honored, session finish/error callbacks fire, and `sendReasoning` / `sendSources` / `sendFinish` filter the Swift UI message stream.
  - API-shape gaps noted: JS exposes separate `result.textStream`, `result.stream`, and deprecated `result.fullStream` handles on one result object. Swift exposes the full language stream directly and exposes text-only projection through `Output.text()`, so tests should be ported to the matching Swift surface instead of forcing JS handle names. JS `result.stream` also injects wrapper-only `start`, `start-step`, `finish-step`, and aggregate `finish` events with performance metrics and fallback response metadata; Swift keeps provider/full stream parts on `LanguageStreamPart` and exposes aggregate lifecycle data through typed telemetry/result structures instead of synthetic stream wrapper events or result-object performance snapshots. JS `experimental_transform` post-processes wrapper `TextStreamPart` chunks, including finish chunks, tool-call input, local tool results emitted after execution, callback snapshots, and explicit stream stopping; Swift has middleware/request/text extraction transforms but no generic public hook at that lifecycle point. JS `experimental_download` generic URL download hooks, including the `prepareStep` model-switch `supportedUrls` download decision in `streamText`, do not have a direct Swift facade surface; Swift covers provider-specific media downloading and `supportedURLs` matching separately. JS result promise/callback helpers such as `consumeStream`, missing-finish `result.text`/`result.steps`/`result.finishReason` promise resolution or rejection behavior, `result.request` retention promises, `result.response.messages`, `onStart` / `onStepStart` / `onStepFinish` / `onChunk` / `onError` / `onEnd` / `onFinish` callback events and aliases, runtime/tools context callback snapshots, and callback-ordering behavior do not have a direct Swift `streamText` result-object surface; Swift represents thrown stream failures through `AsyncThrowingStream` errors, provider error chunks through `LanguageStreamPart.error`, per-step stream loops through `prepareStep` and telemetry, and UI/session finish callbacks through chat/session APIs. JS Node/Web response helpers (`pipeUIMessageStreamToResponse`, `pipeTextStreamToResponse`, `toUIMessageStreamResponse`, `toTextStreamResponse`), SSE headers/chunks, custom response status/header options, error masking hooks, and multiple-consumption behavior are API-shape gaps unless Swift later adds an HTTP/SSE server response layer.

## Pending Generate / Stream Text Ports

None currently pending.

## Pending Object / Media Facade Ports

None currently pending.

## Pending Middleware / Registry / Model Ports

None currently pending.

## Pending Prompt / Upload Ports

None currently pending.

## File-Level API-Shape Gaps

| Upstream file | Reason | Existing Swift coverage |
| --- | --- | --- |
| `packages/ai/src/telemetry/tracing-channel-publisher.test.ts` | Upstream validates Node-only `node:diagnostics_channel` tracing span publication and the Node/non-Node runtime switch. SwiftAISDK currently has no diagnostics-channel or tracing-channel publisher surface. | Swift typed telemetry integrations cover lifecycle recording plus language-model/tool execution wrappers, and the dispatcher tests cover enabled/disabled integration routing. |
| `packages/ai/src/telemetry/tracing-channel.test.ts` | Upstream validates Node `diagnostics_channel` plus `AsyncLocalStorage` span parentage for `generateText`, `streamText`, `embed`, and `rerank`. SwiftAISDK currently exposes typed telemetry events/wrappers, not Node semantic tracing spans or async-local diagnostics-channel parentage. | Existing facade telemetry tests cover generate/stream step/tool ordering, language/tool execution wrapper context, embed/rerank start/end telemetry, abort/error telemetry, and telemetry privacy flags. |
| `packages/ai/src/text-stream/create-text-stream-response.test.ts` | Upstream validates Web `Response` construction, text/plain headers, status/statusText, custom response headers, and encoded response bodies. SwiftAISDK currently has no Web `Response` helper layer. | `toTextStream(_:)` covers the portable text-delta projection used by the response helper; provider HTTP response handling is covered separately by provider tests. |
| `packages/ai/src/text-stream/pipe-text-stream-to-response.test.ts` | Upstream validates Node `ServerResponse` writing, headers, status/statusMessage, and encoded stream chunks. SwiftAISDK currently has no Node `ServerResponse` abstraction or server response writer. | `toTextStream(_:)` covers the portable text-delta projection used by the response helper; lower-level async stream utilities cover Swift stream behavior. |
| `packages/ai/src/ui-message-stream/create-ui-message-stream-response.test.ts` | Upstream validates Web `Response` construction for SSE-encoded UI message chunks, response headers/status, teeing the stream for `consumeSseStream`, and non-blocking/error-swallowing consume behavior. SwiftAISDK currently has no Web `Response` / SSE response helper surface. | Swift UI message behavior is covered at the reducer/chat-transport layer through `AIUIMessageStreamReducer.snapshots` and `AIChatSession`; provider HTTP response handling is covered separately by provider tests. |
| `packages/ai/src/ui-message-stream/create-ui-message-stream.test.ts` | Upstream validates a JS `UIMessageStreamWriter` abstraction over Web `ReadableStream`: synchronous/asynchronous `execute`, `writer.write`, `writer.merge`, multiple merged streams, late writes after close, converting execute/merge errors into error chunks, generated start-message IDs, and `onFinish` / `onEnd` callbacks. SwiftAISDK currently exposes typed `AsyncThrowingStream<LanguageStreamPart>` producers, `AIUIMessageStreamReducer.snapshots`, `DirectAIChatTransport`, and `AIChatSession`, not a public UI-message chunk writer or Web stream merge helper. | The portable output semantics are covered by `AiUIMessageStreamUpstreamTests.swift`, `AIChatSessionTests.swift`, `CoreContractTests.swift`, and stream facade tests: language stream parts build assistant snapshots, continuation IDs are reused, finish/error/abort session callbacks are covered, and Swift stream composition is covered by async stream utility tests. |
| `packages/ai/src/ui-message-stream/pipe-ui-message-stream-to-response.test.ts` | Upstream validates Node `ServerResponse` writing for SSE-encoded UI message chunks, headers, status/statusMessage, and stream error handling. SwiftAISDK currently has no Node `ServerResponse` abstraction or UI-message server response writer. | Swift UI message behavior is covered at the reducer/chat-transport layer through `AIUIMessageStreamReducer.snapshots` and `AIChatSession`; lower-level async stream utilities cover Swift stream behavior. |
| `packages/ai/src/ui-message-stream/to-ui-message-chunk.test.ts` | Upstream validates the JS internal mapper from `TextStreamPart` object unions to `UIMessageChunk` wire objects, including start/finish/start-step/finish-step chunks, message metadata chunks, source-url/source-document chunk variants, invalid-tool chunks, and callback error-text conversion. SwiftAISDK has no public UI-message chunk union; it maps typed `LanguageStreamPart` values directly into `AIUIMessagePart` and message metadata through `AIUIMessageStreamReducer`. | `CoreContractTests.swift`, `AiUIMessageStreamUpstreamTests.swift`, provider stream tests, and stream facade tests cover the portable semantics: text/reasoning/file/source/custom/tool/error/finish parts preserve provider metadata where Swift exposes it, sources can be filtered at `DirectAIChatTransport`, tool input/result/approval parts build typed UI message parts, and finish metadata is retained on assistant messages. |
| `packages/ai/src/ui/http-chat-transport.test.ts` | Upstream validates browser/server HTTP chat transport request construction: `api`, static/function `body`, static/function `headers`, POST request JSON, and Web response stream processing hooks. SwiftAISDK currently exposes the generic `AIChatTransport` protocol plus `DirectAIChatTransport`, not an HTTP/fetch-backed chat transport implementation. | `AIChatSessionTests.swift` covers forwarding `headers`, `body`, and metadata into `AIChatTransportRequest`; provider HTTP request serialization is covered by provider tests. A future Swift HTTP chat transport should port these tests when that surface exists. |
| `packages/ai/src/ui/last-assistant-message-is-complete-with-approval-responses.test.ts` | Upstream validates a JavaScript helper predicate over dynamic UI tool parts, string `state` values (`approval-requested`, `approval-responded`, `output-available`), provider-executed dynamic tools, and `step-start` last-step boundaries. SwiftAISDK models tool approvals as separate typed `.toolApprovalRequest` / `.toolApprovalResponse` parts and exposes auto-send policy as the user-supplied `sendAutomaticallyWhen` callback rather than this helper predicate. | `AIChatSessionTests.swift` covers adding tool approval responses and invoking `sendAutomaticallyWhen`; `CoreContractTests.swift` covers typed approval request/response validation and UI-message-to-model-message conversion. |
| `packages/ai/src/ui/last-assistant-message-is-complete-with-tool-calls.test.ts` | Upstream validates a JavaScript helper predicate over dynamic/static UI tool parts, string tool states (`input-streaming`, `input-available`, `output-available`, `output-error`), provider-executed tools, and `step-start` last-step boundaries. SwiftAISDK represents calls/results as typed `.toolCall` / `.toolResult` parts and leaves automatic continuation criteria to the user-supplied `sendAutomaticallyWhen` callback, so there is no equivalent public predicate surface to port directly. | `AIChatSessionTests.swift` covers automatic sending after a tool output through `sendAutomaticallyWhen`; `CoreContractTests.swift` covers typed tool call/result validation, provider stream conversion, and model-message conversion. |
| `packages/ai/src/ui/process-text-stream.test.ts` | Upstream validates a browser `ReadableStream<Uint8Array>` text decoder helper that calls a callback for decoded text chunks. SwiftAISDK currently has no UI byte-stream decoder helper; text stream utilities operate on typed `LanguageStreamPart` values or provider HTTP bodies inside provider implementations. | `AiTextStreamUpstreamTests.swift` covers the portable typed text-delta projection through `toTextStream(_:)`; provider byte/body decoding is covered by provider streaming tests. |
| `packages/ai/src/ui/process-ui-message-stream.test.ts` | Upstream validates the JS `ReadableStream<UIMessageChunk>` processor: mutable streaming state, `runUpdateMessageJob`, callback timing, chunk-level schemas, JS prototype-pollution guards, dynamic/static tool state strings, provider-executed tool state transitions, approval chunk states, transient data parts, and custom chunk handling. SwiftAISDK has no public UI-message chunk stream processor; it consumes typed `LanguageStreamPart` values through `AIUIMessageStreamReducer.snapshots` and chat transports. | `AiUIMessageStreamUpstreamTests.swift`, `CoreContractTests.swift`, and provider stream tests cover portable typed semantics for text/reasoning/tool/file/source/metadata/custom/error/finish parts, reducer validation for missing starts, provider metadata propagation, and chat-session finish/error/abort behavior. Remaining JS chunk-stream callback/schema/prototype/dynamic-state cases do not have an equivalent Swift surface. |
| `packages/ai/src/ui/transform-text-to-ui-message-stream.test.ts` | Upstream validates converting a JS `ReadableStream<string>` into synthetic `UIMessageChunk` wire events (`start`, `start-step`, text lifecycle, `finish-step`, `finish`). SwiftAISDK has no public synthetic UI chunk stream layer; text-to-UI conversion is represented by typed `LanguageStreamPart` streams consumed by `AIUIMessageStreamReducer.snapshots`. | `AiUIMessageStreamUpstreamTests.swift`, `AiTextStreamUpstreamTests.swift`, and `CoreContractTests.swift` cover the portable typed semantics: text deltas accumulate into assistant UI message snapshots, empty/non-text parts are ignored by `toTextStream(_:)`, and finish metadata is retained when present. |
| `packages/ai/src/ui/ui-messages.test.ts` | Upstream validates JavaScript helper predicates over dynamic UI part object unions and string-prefixed tool/data part types (`getStaticToolName`, `isCustomContentUIPart`, `isDataUIPart`). SwiftAISDK models UI parts as the typed `AIUIMessagePart` enum and typed structs, so callers use Swift pattern matching rather than string-prefix runtime type guards. | `CoreUIMessages.swift`, `CoreContractTests.swift`, and validation/conversion tests cover typed `.custom`, `.data`, `.toolCall`, `.toolResult`, and related UI message parts. |
| `packages/ai/src/realtime/browser-realtime-transport.test.ts` | Upstream validates browser `WebSocket` transport behavior for realtime sessions: connecting/disconnecting sockets, late-open suppression, reconnect close behavior, and async client-event serialization ordering. SwiftAISDK currently has no browser realtime WebSocket transport surface. | Swift chat transport and MCP/HTTP transports are covered separately, but they do not expose upstream realtime WebSocket semantics. |
| `packages/ai/src/realtime/realtime-event-reducer.test.ts` | Upstream validates the JS realtime event reducer contract for realtime server events: streamed assistant text assembly, function-call argument state/tool output, and delayed input transcription insertion. SwiftAISDK currently has no realtime event model/reducer surface. | Swift UI message reducer and chat session tests cover language-stream-to-UI message behavior, but not realtime server event unions or realtime tool-output effects. |
| `packages/ai/src/realtime/realtime-session.test.ts` | Upstream validates abstract realtime session behavior around tool-call handler absence/manual flow, function-call output submission, and response-create timing. SwiftAISDK currently has no abstract realtime session API. | Swift chat session tests cover submit/regenerate/tool-output flows for the existing chat transport, but not upstream realtime session transport contracts. |

## Pending Realtime / Telemetry / Stream Utility Ports

None currently pending.

## Partial UI Message Stream Coverage

- `packages/ai/src/ui-message-stream/read-ui-message-stream.test.ts`
  - `AiUIMessageStreamUpstreamTests.swift`, `AIUIMessageStreamReducer.swift`
  - Ported the portable reducer semantics for the basic input stream: stream start yields an assistant message snapshot, text start/deltas/end produce streaming and done `AIUITextPart` snapshots, and the Swift final `finish` part records `finishReason` metadata on the final snapshot.
  - Ported `terminateOnError` semantics as `AIUIMessageStreamReducer.snapshots(..., terminateOnError: true)`, which throws `AIUIMessageStreamError` when an error stream part is encountered.
  - Remaining upstream snapshot details are chunk-wire-shape differences: JS exposes `UIMessageChunk` objects and `step-start` UI parts; Swift currently consumes typed `LanguageStreamPart` directly and has no separate public UI-message chunk protocol.
- `packages/ai/src/ui-message-stream/to-ui-message-stream.test.ts`
  - `AiUIMessageStreamUpstreamTests.swift`, existing `CoreContractTests.swift`
  - Covered the portable text/lifecycle stream-to-UI-message behavior through `AIUIMessageStreamReducer.snapshots` and `DirectAIChatTransport`: text stream parts become UI message snapshots, finish metadata can be retained, and reasoning/source/finish forwarding can be filtered by Swift transport options.
  - Remaining upstream cases depend on JS-specific UI chunk output (`start`, `message-metadata`, `finish-step`, generated start chunk IDs, and `onFinish` / `onEnd` callbacks) rather than Swift's direct `AIUIMessage` snapshot stream.
- `packages/ai/src/ui-message-stream/get-response-ui-message-id.test.ts`
  - `AIChatSessionTests.swift`, `AIChatSession.swift`
  - Ported the last-assistant continuation behavior on the real Swift chat-session surface: submitting an existing transcript whose last message is assistant reuses that assistant message ID as the response ID, does not call the generator, sends the original assistant message in the request, and upserts the streamed snapshot into the same message.
  - Existing `AIChatSessionTests.swift` covers the adjacent last-user case where a new response ID is generated, and explicit `responseMessageID` transport handling covers the string-ID branch. The upstream `originalMessages: undefined` non-persistence branch has no direct Swift session equivalent.
- `packages/ai/src/ui-message-stream/handle-ui-message-stream-finish.test.ts`
  - `AIChatSessionTests.swift`, `AIChatSession.swift`, `AIUIMessageStreamReducer.swift`
  - Covered portable finalization behavior through Swift chat sessions: successful finish callbacks receive final message/messages and non-error flags, stream errors invoke error plus finish callbacks with `isError`, continuation reuses the existing assistant message ID, and explicit `stop()` maps to upstream abort finalization by calling `onFinish` once with `isAbort`.
  - Remaining upstream cases are tied to the JS `UIMessageChunk` transform layer: pass-through chunk identity, start-chunk message ID injection, `abort` chunks and abort reasons, browser reader cancellation, `finish-step`/`start-step` step callbacks, deprecated callback alias precedence, swallowed async callback errors, and deep-clone mutation isolation.
- `packages/ai/src/ui/direct-chat-transport.test.ts`
  - `CoreContractTests.swift`, `AIAgentTests.swift`, `AIChatSessionTests.swift`, `AIChatTransport.swift`
  - Covered through Swift `DirectAIChatTransport` and agent/chat-session tests: model streams become UI message snapshots, abort signals and request options are forwarded to the model, `sendReasoning` / `sendSources` / `sendFinish` filter the UI stream, UI messages are converted to model messages, validation failures throw typed errors, and `reconnectToStream` defaults to `nil`.
  - Upstream JS `ToolLoopAgent` option object / `prepareCall` exact shape and Web `ReadableStream<UIMessageChunk>` assertions map to Swift's typed `AIChatRequestOptions`, `AIPrepareStep`, `AsyncThrowingStream<AIUIMessage>`, and typed model request surfaces.
- `packages/ai/src/ui/convert-to-model-messages.test.ts`
  - `AiConvertToModelMessagesUpstreamTests.swift`, `AIUIMessageConversion.swift`, existing `CoreContractTests.swift`
  - Ported upstream simple text message conversion, system text provider metadata promotion, multi-part system text/provider metadata merging, user/assistant text provider metadata preservation, assistant reasoning part conversion with per-part provider metadata, typed assistant tool output splitting, typed provider-executed tool output placement, typed denied approval execution-denied result conversion, data-part skipping when no converter is provided, custom assistant part conversion, and conversation order preservation.
  - Ported assistant file part conversion for Swift inline file data, including filename and provider metadata preservation, and user/assistant provider-reference file conversion.
  - Ported the upstream 7.0.8 persistent data-before-model-stream regression: assistant UI messages with data parts before model text do not emit an empty assistant model message; only the converted text content is preserved.
  - Runtime/API fixes from this pass: Swift UI `.custom` parts now become model-message `.custom` content parts with provider metadata preserved; system text parts are concatenated and their provider metadata is promoted to `AIMessage.providerMetadata`, matching provider-level system metadata usage; assistant reasoning UI parts now become model-message `.reasoning` content parts instead of message-level aggregate reasoning; typed assistant tool results now split into follow-up tool model messages unless provider-executed; denied approval responses synthesize execution-denied tool results when no explicit tool result is present; UI file parts can carry provider references through `AIStreamFile.providerReference` and model-message `.providerReference` now preserves filename.
  - Existing coverage already maps simple text/file/tool conversion, provider metadata preservation, unsupported URL-file rejection, and direct chat transport history conversion.
  - Remaining upstream cases are classified as API-shape gaps: JS dynamic/static tool-state UI objects (`input-streaming`, `input-available`, `output-denied`, `approval-responded`), `ignoreIncompleteToolCalls`, `convertDataPart` callbacks, unhandled string roles, and `step-start` grouping. Swift represents these as typed tool call/result/approval parts, enum roles, and direct message arrays rather than mutable JS UI part state objects.
- `packages/ai/src/ui/validate-ui-messages.test.ts`
  - `AiValidateUIMessagesUpstreamTests.swift`, `AIUIMessageValidation.swift`, existing `CoreContractTests.swift`
  - Ported the upstream custom assistant part validation cases, with and without provider metadata, against both throwing and safe validation surfaces.
  - Ported additional portable validation cases for message metadata without a schema, reasoning parts, source URL parts, source document parts, file parts, provider-reference file preservation, and data parts.
  - Existing coverage already maps empty message/parts rejection, broken tool-call/result/approval references, typed approval errors, and basic supported UI message parts.
  - Remaining upstream cases include metadata schemas, data-part schemas, dynamic/static JS tool state validation, source-document wire shape, step-start parts, safe validation error envelopes, and Zod-specific error formatting. Some are Swift API-shape gaps because Swift currently has typed parts without runtime schema validators for message metadata/data/tool input/output.
- `packages/ai/src/ui/chat.test.ts`
  - `AIChatSessionTests.swift`, `AIChatSession.swift`, existing UI stream/reducer coverage
  - Ported the upstream user text-message metadata case and `clearError` status reset case on the Swift chat-session surface.
  - Existing coverage already maps sending messages, request option forwarding, replacing user messages, submitting an existing transcript, last-assistant continuation, regeneration, resume/reconnect, finish/error callbacks, abort finalization, tool output/approval insertion, auto-send after tool output, and option forwarding when approval responses auto-send.
  - Remaining upstream cases include JS `DefaultChatTransport` HTTP request/response details, chunk-level streaming history snapshots, disconnected stream handling, dynamic tool-state helper predicates, tool approval mutation-in-place semantics, and promise-returning `sendAutomaticallyWhen`. Swift exposes typed `AIChatTransport`, typed message parts, and synchronous `sendAutomaticallyWhen`.

## Pending UI Message Ports

None currently pending.

## Pending Utility / Test Helper Ports

None currently pending.

## Ported Test Helper Parity

| Upstream file | Swift evidence | Notes |
| --- | --- | --- |
| `packages/ai/src/test/mock-embedding-model.test.ts` | `AiMockModelUpstreamTests.swift` | Covered against Swift test support `MockEmbeddingModel`: array-backed embed results are returned sequentially and requests are recorded. Upstream V3/V4 helper split maps to the single current Swift `EmbeddingModel` protocol helper. |
| `packages/ai/src/test/mock-language-model.test.ts` | `AiMockModelUpstreamTests.swift` | Covered against Swift test support `MockLanguageModel`: array-backed generate and stream results are returned sequentially and stream calls are recorded. Upstream V2/V3/V4 helper split maps to the single current Swift `LanguageModel` protocol helper. |

## Out Of Scope Or Requires Separate Design

- `packages/ai/src/model/as-embedding-model-v3.test.ts` - JS V2-to-V3 adapter helper. Swift exposes one current `EmbeddingModel` protocol and has no versioned adapter layer.
- `packages/ai/src/model/as-embedding-model-v4.test.ts` - JS V2/V3-to-V4 adapter helper. Swift exposes one current `EmbeddingModel` protocol and has no versioned adapter layer.
- `packages/ai/src/model/as-image-model-v3.test.ts` - JS V2-to-V3 adapter helper. Swift exposes one current `ImageModel` protocol and has no versioned adapter layer.
- `packages/ai/src/model/as-image-model-v4.test.ts` - JS V2/V3-to-V4 adapter helper. Swift exposes one current `ImageModel` protocol and has no versioned adapter layer.
- `packages/ai/src/model/as-language-model-v3.test.ts` - JS V2-to-V3 adapter helper. Swift exposes one current `LanguageModel` protocol and has no versioned adapter layer.
- `packages/ai/src/model/as-language-model-v4.test.ts` - JS V2/V3-to-V4 adapter helper. Swift exposes one current `LanguageModel` protocol and has no versioned adapter layer.
- `packages/ai/src/model/as-provider-v4.test.ts` - JS provider V2/V3-to-V4 adapter helper. Swift providers conform directly to the current `AIProvider` protocol; provider routing parity is covered by `wrapProvider`, `customProvider`, `createProviderRegistry`, and `AI.resolve*Model` tests.
- `packages/ai/src/model/as-reranking-model-v4.test.ts` - JS V3-to-V4 adapter helper. Swift exposes one current `RerankingModel` protocol and has no versioned adapter layer.
- `packages/ai/src/model/as-speech-model-v3.test.ts` - JS V2-to-V3 adapter helper. Swift exposes one current `SpeechModel` protocol and has no versioned adapter layer.
- `packages/ai/src/model/as-speech-model-v4.test.ts` - JS V2/V3-to-V4 adapter helper. Swift exposes one current `SpeechModel` protocol and has no versioned adapter layer.
- `packages/ai/src/model/as-transcription-model-v3.test.ts` - JS V2-to-V3 adapter helper. Swift exposes one current `TranscriptionModel` protocol and has no versioned adapter layer.
- `packages/ai/src/model/as-transcription-model-v4.test.ts` - JS V2/V3-to-V4 adapter helper. Swift exposes one current `TranscriptionModel` protocol and has no versioned adapter layer.
- `packages/ai/src/model/as-video-model-v4.test.ts` - JS V3-to-V4 adapter helper. Swift exposes one current `VideoModel` protocol and has no versioned adapter layer.
- `packages/ai/internal/index.test.ts` - JS internal deprecated alias (`prepareCallSettings`) for `prepareLanguageModelCallOptions`. Swift exposes `prepareLanguageModelCallOptions(_:)` directly and has no internal package-export alias layer or JS runtime non-integer validation branch to mirror.
- `packages/ai/src/agent/create-agent-ui-stream-response.test.ts` - Web `Response` / SSE helper for agent UI streams. Swift currently exposes `createAgentUIStream(...)` as an `AsyncThrowingStream<AIUIMessage>` and `DirectAIChatTransport`/`AIChatSession` for UI-message consumption, but has no HTTP `Response` writer, SSE chunk serializer, `onFinish` Web callback surface, or sandbox parameter on agent response helpers. Portable UI stream/message conversion behavior and `AITool.toModelOutput` prompt behavior are covered by `AIAgentTests.swift`, UI message stream tests, `AiPromptToolOutputUpstreamTests.swift`, and `AiToResponseMessagesUpstreamTests.swift`.
- `packages/ai/src/prompt/file-part-data.test.ts` - JS helper normalizes untyped/legacy `FilePart.data` union values (`Uint8Array`, `ArrayBuffer`, URL strings, `URL` instances, provider-reference-shaped objects, and tagged `{type:*}` objects) into the V4 tagged data shape. SwiftAISDK exposes typed `AIContentPart` cases instead: `.data`, `.file`, `.imageURL`, `.providerReference`, and `.text`; inline bytes are already `Data`, URLs cannot be confused with base64 strings, and provider references use `AIProviderReference`. Provider-specific serialization and data URL handling are covered in prompt/provider tests such as Anthropic media prompt parity, OpenAI Responses input conversion, Bedrock Anthropic data URL handling, and `AIContentPart.filePayload`-using provider tests. |
- `packages/ai/src/util/create-id-map.test.ts` - JS null-prototype object map helper that prevents `Object.prototype` lookup/pollution. Swift `Dictionary` has no prototype chain and already stores keys such as `__proto__`, `constructor`, and `prototype` as ordinary string keys.
- `packages/ai/src/util/write-to-server-response.test.ts` - Node `http.ServerResponse` integration helper that writes web-stream chunks with `writeHead`, `write`, `drain`, and `end` backpressure semantics. The Swift port currently has no server response abstraction equivalent to Node's `ServerResponse`; response streaming surfaces are covered separately by Swift-native stream utilities.
