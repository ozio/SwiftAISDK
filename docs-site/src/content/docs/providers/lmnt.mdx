---
title: "lmnt"
description: "lmnt provider capabilities and factories."
---

## Package

`@ai-sdk/lmnt`

## Factories

`AIProviders.lmnt`

## Capabilities

- Speech

## Notes

LMNT speech mirrors upstream 3.0.5: `LMNT_API_KEY`, `x-api-key` auth, custom headers, versioned user-agent suffix, `speech`/`speechModel` aliases, default voice `ava`, `/v1/ai/speech/bytes`, binary audio responses, output format warnings/fallback, `providerOptions.lmnt` speech schema defaults, response metadata, and unsupported language/embedding/image families.

Factory argument requirements are defined by the public Swift factory signatures. Use [Public symbols](../reference/generated/public-symbols/) when you need the exact initializer or factory declaration.

Return to the [provider matrix](./).
