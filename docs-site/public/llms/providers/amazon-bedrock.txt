# "amazon-bedrock"

Source URL: /providers/amazon-bedrock/

## Package

`@ai-sdk/amazon-bedrock`

## Factories

`AIProviders.amazonBedrock`

## Capabilities

- Language
- Embedding
- Image
- Reranking

## Notes

Amazon Bedrock Converse, embeddings, images, and reranking mirror upstream 5.0.68: SigV4 and bearer auth, versioned user-agent suffix, Converse request/stream parsing, ordered generated content with signature, legacy redacted-data, and current redacted-content replay metadata, event-stream decode/process failure propagation and incomplete-frame EOF rejection, empty tool-call ID normalization, provider options under `amazonBedrock`/`bedrock`, service tiers, guardrails, top-level reasoning mapping, cache points, Converse inline and S3 video sources across ten supported MIME types, document citations, rich tool-result content, strict-tool warnings and JSON response fallback that preserves ordinary tools, Cohere batch embeddings up to 96 inputs, Nova/Titan/Cohere embedding response shapes, and the current `bedrockRerankingConfiguration` rerank request key. The latest pass omits assistant turns left empty after unsigned-reasoning filtering, and also preserves exact case-sensitive `s3://` image sources in user and tool-result content, sanitizes replayed tool names, percent-encodes ARN model paths, and omits strict/native structured-output fields for Claude families that Bedrock rejects. The 5.0.68 pass also adds inference-profile reasoning, cache-only assistant omission, ARN embedding-family detection, citations, parallel-tool disabling, guardrail block handling, complete raw usage, model-specific reasoning shapes, and typed stream errors. ProviderV4 type names, ESM-only packaging, Node 22 engines, optional JS headers config, and workflow serialization helpers are JS-only upstream concerns.

Factory argument requirements are defined by the public Swift factory signatures. Use [Public symbols](../reference/generated/public-symbols/) when you need the exact initializer or factory declaration.

Return to the [provider matrix](./).
