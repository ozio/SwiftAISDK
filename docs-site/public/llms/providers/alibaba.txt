# "alibaba"

Source URL: /providers/alibaba/

## Package

`@ai-sdk/alibaba`

## Factories

`AIProviders.alibaba`

## Capabilities

- Language
- Embedding
- Video

## Notes

Alibaba mirrors upstream 2.0.39: `ALIBABA_API_KEY`, versioned user-agent suffix, chat/language callable aliases, DashScope chat completions with thinking/reasoning mapping, generated fallback IDs for missing tool-call IDs, cache-aware usage with nonnegative reasoning-adjusted output text tokens, historical assistant reasoning omission including removal of reasoning-only turns, DashScope embedding endpoint with dense and sparse option mapping, video submit/poll flow, wan2.6 legacy video protocol, wan2.7 media/ratio video protocol, WAN 3 all-in-one media/resolution/aspect-ratio/audio mapping and usage metadata, provider option validation, warnings, provider metadata, and unsupported image families. ProviderV4 type names, ESM-only packaging, Node 22 engines, and workflow serialization helpers are JS-only upstream concerns.

Factory argument requirements are defined by the public Swift factory signatures. Use [Public symbols](../reference/generated/public-symbols/) when you need the exact initializer or factory declaration.

Return to the [provider matrix](./).
