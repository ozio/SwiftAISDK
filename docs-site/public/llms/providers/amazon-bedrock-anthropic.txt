# "amazon-bedrock.anthropic"

Source URL: /providers/amazon-bedrock-anthropic/

## Package

`@ai-sdk/amazon-bedrock`

## Factories

`AIProviders.amazonBedrockAnthropic`

## Capabilities

- Language

## Notes

Bedrock Anthropic mirrors upstream 5.0.82 invoke-model behavior: Anthropic Messages request conversion, Bedrock auth and user-agent handling, URL/data media resolution, native Anthropic tool helpers, model-specific structured-output and strict-tool omissions, event-stream decoding that surfaces decode/process failures and rejects incomplete frames at EOF, response metadata, and Bedrock-specific request URLs. JS fetch/header injection hooks remain JS-only. The 5.0.76 pass shares the new structured-output mode selection, model capability gates, document sanitization, citation extraction, and provider-executed tool-result replay.

Factory argument requirements are defined by the public Swift factory signatures. Use [Public symbols](/reference/generated/public-symbols/) when you need the exact initializer or factory declaration.

Return to the [provider matrix](/providers/).
