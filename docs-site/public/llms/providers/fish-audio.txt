# "fish-audio"

Source URL: /providers/fish-audio/

## Package

`@ai-sdk/fish-audio`

## Factories

`AIProviders.fishAudio`

## Capabilities

- Transcription
- Speech

## Notes

Fish Audio mirrors upstream 3.0.7: `FISH_AUDIO_API_KEY`, bearer auth, custom base URL and headers, versioned user-agent suffix, binary `/v1/tts` speech generation with model headers, output formats, latency/normalization/sampling controls and warnings, plus multipart `/v1/asr` transcription with language, duration, and timed text segments. Request/response metadata, abort propagation, and Fish `{status,message}` error handling follow the published package.

Factory argument requirements are defined by the public Swift factory signatures. Use [Public symbols](../reference/generated/public-symbols/) when you need the exact initializer or factory declaration.

Return to the [provider matrix](./).
